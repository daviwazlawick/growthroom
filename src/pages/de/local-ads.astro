---
const CHECKOUT_URL = "https://alfima.com/daviwazl/ads-blueprint/purchase";
// TODO: publish when real testimonials are collected. Never invent testimonials, names, or photos.
const SHOW_TESTIMONIALS = false;
---

<!DOCTYPE html>
<html lang="de">
<head>
<!-- Google Tag Manager -->
<script is:inline>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
})(window,document,'script','dataLayer','GTM-KL573474');</script>
<!-- End Google Tag Manager -->
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Ads Blueprint — Lerne, deine Anzeigen selbst zu schalten</title>
<meta name="description" content="Der komplette Blueprint, um Facebook- & Instagram-Anzeigen selbst zu erstellen und zu schalten — von deiner allerersten Kampagne bis zu echten Ergebnissen. Einmalig 27 €.">
<link rel="canonical" href="https://growthroom.eu/de/local-ads">
<link rel="alternate" hreflang="en" href="https://growthroom.eu/local-ads">
<link rel="alternate" hreflang="de" href="https://growthroom.eu/de/local-ads">
<link rel="alternate" hreflang="x-default" href="https://growthroom.eu/local-ads">
<script is:inline>
(function(){try{
  if (localStorage.getItem("gr_lang") === "en") location.replace(location.pathname.replace(/^\/de/, "") + location.search);
}catch(e){}})();
</script>

<!-- Meta Pixel: PageView dispara sempre no load; demais eventos exigem consentimento -->
<script>
  (function () {
    !function(f,b,e,v,n,t,s){if(f.fbq)return;n=f.fbq=function(){n.callMethod?
    n.callMethod.apply(n,arguments):n.queue.push(arguments)};if(!f._fbq)f._fbq=n;
    n.push=n;n.loaded=!0;n.version='2.0';n.queue=[];t=b.createElement(e);t.async=!0;
    t.src=v;s=b.getElementsByTagName(e)[0];s.parentNode.insertBefore(t,s)}(window,
    document,'script','https://connect.facebook.net/en_US/fbevents.js');
    fbq('init', '1009429968103557');
    fbq('track', 'PageView');
  })();

  // Gate de consentimento: vale para TODOS os eventos depois do PageView.
  var CONSENT_KEY = "gr_cookie_consent_v1";
  try { window.__marketingConsent = localStorage.getItem(CONSENT_KEY) === "accepted"; }
  catch (e) { window.__marketingConsent = false; }
  window.trackMarketing = function (event, params, options) {
    if (!window.__marketingConsent || typeof window.fbq !== "function") return;
    window.fbq("track", event, params || {}, options || {});
  };
</script>
<!-- End Meta Pixel -->

<!-- Google tag (gtag.js): Analytics gated by consent via Consent Mode -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-TRFL8MQJ5R"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  // Consent Mode: analytics negado por padrão; liberado só após o "Accept".
  gtag('consent', 'default', { 'analytics_storage': 'denied' });
  // Se o usuário já consentiu numa visita anterior, libera de imediato.
  if (window.__marketingConsent) {
    gtag('consent', 'update', { 'analytics_storage': 'granted' });
  }

  gtag('config', 'G-TRFL8MQJ5R');
</script>
<!-- End Google tag -->

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;1,9..40,400&family=DM+Mono:wght@400&display=swap" rel="stylesheet">
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --bg:       #F7F5F2;
    --white:    #FFFFFF;
    --ink:      #1A1A1A;
    --ink2:     #333333;
    --ink3:     #555555;
    --muted:    #888888;
    --faint:    #AAAAAA;
    --border:   #E0DDD8;
    --border2:  #EEECEA;
    --tag-bg:   #EDEBE7;
    --accent:      #F5A623;
    --accent-dark: #D4880A;
    --accent-soft: #FFF4E0;
  }

  html, body { overflow-x: hidden; max-width: 100%; }
  html { font-family: 'DM Sans', system-ui, sans-serif; font-size: 16px; -webkit-font-smoothing: antialiased; background: var(--bg); color: var(--ink); }

  a { color: inherit; text-decoration: none; }

  /* ── Layout ── */
  .wrap { max-width: 760px; margin: 0 auto; padding-left: 24px; padding-right: 24px; }
  section { border-bottom: 1px solid var(--border); }
  .s-white { background: var(--white); }
  .s-bg    { background: var(--bg); }

  /* ── Type scale ── */
  .eyebrow { font-size: 11px; letter-spacing: .1em; text-transform: uppercase; color: var(--accent-dark); font-weight: 600; display: block; margin-bottom: 14px; }
  .h1 { font-size: clamp(30px, 5vw, 50px); font-weight: 600; line-height: 1.12; letter-spacing: -.02em; color: var(--ink); margin-bottom: 20px; overflow-wrap: break-word; }
  .h2 { font-size: clamp(24px, 4vw, 38px); font-weight: 600; line-height: 1.15; letter-spacing: -.018em; color: var(--ink); margin-bottom: 28px; overflow-wrap: break-word; }
  .lead { font-size: 18px; color: var(--ink3); line-height: 1.6; margin-bottom: 12px; }
  .body { font-size: 15px; color: var(--ink3); line-height: 1.7; }
  .small { font-size: 13px; color: var(--ink3); line-height: 1.6; }
  .tiny  { font-size: 11px; color: var(--muted); }
  .mono  { font-family: 'DM Mono', monospace; }

  /* ── Buttons ── */
  .btn {
    display: inline-block;
    background: linear-gradient(180deg, #FFBE5C 0%, var(--accent) 55%, #F09B10 100%);
    color: #1A1A1A;
    padding: 16px 32px;
    font-size: 15px;
    font-weight: 700;
    letter-spacing: .02em;
    border-radius: 12px;
    cursor: pointer;
    transition: transform .12s, box-shadow .15s, filter .15s;
    border: none;
    box-shadow: 0 12px 26px -10px rgba(245,166,35,.75), inset 0 1px 0 rgba(255,255,255,.45);
  }
  .btn:hover { filter: brightness(.96); transform: translateY(-1px); box-shadow: 0 14px 30px -10px rgba(212,136,10,.8), inset 0 1px 0 rgba(255,255,255,.3); }
  .btn:active { transform: translateY(0); }
  .btn-sm { padding: 14px 26px; font-size: 14px; }

  /* ── Language switch ── */
  .lang-switch { position: absolute; top: 14px; right: 14px; z-index: 50; display: flex; gap: 2px; background: rgba(255,255,255,.75); border: 1px solid var(--border); border-radius: 999px; padding: 3px; }
  .lang-switch a, .lang-switch span { font-size: 16px; line-height: 1; padding: 5px 7px; border-radius: 999px; opacity: .45; cursor: pointer; text-decoration: none; }
  .lang-switch .active { opacity: 1; background: var(--white); cursor: default; }
  .lang-switch a:hover { opacity: .85; }

  /* ── Free lesson link ── */
  .link-free {
    display: inline-block; margin-top: 16px;
    font-size: 13px; color: var(--ink3); font-family: inherit;
    text-decoration: underline; text-underline-offset: 3px;
    background: none; border: none; padding: 0; cursor: pointer;
    transition: color .15s;
  }
  .link-free:hover { color: var(--accent-dark); }

  /* ── Free class modal ── */
  #fc-overlay {
    position: fixed; inset: 0; z-index: 10000;
    background: rgba(26,26,26,.55);
    display: none; align-items: flex-start; justify-content: center;
    overflow-y: auto; padding: 24px;
  }
  #fc-overlay.open { display: flex; }
  .fc-modal { background: var(--white); border: 1px solid var(--border); border-top: 3px solid var(--accent); border-radius: 16px; max-width: 640px; width: 100%; margin: auto; position: relative; padding: 32px 28px; }
  @media (max-width: 480px) { .fc-modal { padding: 26px 20px; } #fc-overlay { padding: 14px; } }
  .fc-close { position: absolute; top: 12px; right: 12px; background: none; border: none; font-size: 15px; color: var(--muted); cursor: pointer; padding: 6px; transition: color .15s; }
  .fc-close:hover { color: var(--ink); }
  .fc-field { margin-bottom: 14px; }
  .fc-field label { display: block; font-size: 12px; font-weight: 600; color: var(--ink2); margin-bottom: 6px; }
  .fc-field input { width: 100%; border: 1px solid var(--border); background: var(--bg); padding: 13px 15px; font-size: 15px; font-family: inherit; color: var(--ink); border-radius: 10px; }
  .fc-field input:focus { outline: 2px solid var(--accent); outline-offset: -1px; }
  .fc-error { display: none; font-size: 12px; color: #C0392B; margin-top: 10px; }
  .fc-video video { width: 100%; display: block; background: #000; aspect-ratio: 16 / 9; border: 1px solid var(--border); border-radius: 10px; }
  .fc-lessons { border: 1px solid var(--border); border-radius: 10px; overflow: hidden; margin-top: 20px; }
  #fc-offer { border-radius: 14px; }
  .fc-lesson { display: flex; gap: 12px; align-items: center; padding: 11px 16px; border-top: 1px solid var(--border2); font-size: 13px; color: var(--ink2); }
  .fc-lesson:first-child { border-top: none; }
  .fc-lesson.locked { color: var(--muted); background: var(--bg); }
  .fc-lesson.fc-playable { cursor: pointer; transition: background .15s; }
  .fc-lesson.fc-playable:hover { background: var(--accent-soft); }
  .fc-lesson.fc-playable.active .fc-tag { color: var(--accent-dark); font-weight: 600; }
  .fc-lesson.locked { cursor: pointer; transition: background .15s; }
  .fc-lesson.locked:hover { background: var(--accent-soft); }
  .fc-lock-note { display: none; padding: 16px; background: var(--accent-soft); border-top: 1px solid var(--border2); }
  .fc-lock-note p { font-size: 13px; color: var(--ink2); line-height: 1.55; margin-bottom: 12px; }
  .fc-lock-note strong { color: var(--ink); }
  .fc-lesson .mono { font-size: 11px; color: var(--accent-dark); flex-shrink: 0; }
  .fc-lesson .fc-tag { margin-left: auto; font-size: 11px; flex-shrink: 0; }
  .fc-attn { animation: fcPulse 1.2s ease-in-out 3; }
  @keyframes fcPulse {
    0%, 100% { box-shadow: 0 0 0 0 rgba(245,166,35,0); }
    50%      { box-shadow: 0 0 0 6px rgba(245,166,35,.45); }
  }

  /* ── Emphasis ── */
  .prose em, .body em, .lead em { color: var(--accent-dark); font-style: italic; font-weight: 500; }

  /* ── Video embed ── */
  .video-frame { position: relative; width: 100%; aspect-ratio: 16 / 9; border: 1px solid var(--border); background: #000; overflow: hidden; }
  .video-frame iframe { position: absolute; inset: 0; width: 100%; height: 100%; border: 0; }
  .video-cap { font-size: 12px; color: var(--muted); margin-top: 12px; }

  /* ── Hero video (self-hosted) ── */
  .hero-video { position: relative; width: 100%; aspect-ratio: 16 / 9; border: 1px solid var(--border); background: #000; overflow: hidden; }
  .hero-video video { position: absolute; inset: 0; width: 100%; height: 100%; display: block; object-fit: cover; background: #000; }
  .hv-play { position: absolute; inset: 0; display: flex; align-items: center; justify-content: center; width: 100%; border: 0; padding: 0; background: rgba(26,26,26,.30); cursor: pointer; transition: background .18s ease; }
  .hv-play:hover { background: rgba(26,26,26,.42); }
  .hv-play span { display: inline-flex; align-items: center; gap: 10px; background: var(--white); color: var(--ink); font-size: 15px; font-weight: 600; padding: 13px 22px; border-radius: 999px; box-shadow: 0 12px 34px -12px rgba(0,0,0,.55); }
  .hero-video.is-playing .hv-play { display: none; }
  @media (max-width: 600px) { .hv-play span { font-size: 13px; padding: 11px 18px; } }

  /* ── Inline figures ── */
  .figure { margin: 28px 0 0; }
  .figure img { width: 100%; display: block; border: 1px solid var(--border); }
  .figure figcaption { font-size: 12px; color: var(--muted); margin-top: 10px; }
  /* portrait/tall images — keep them small and centered */
  .figure-portrait { max-width: 360px; margin-left: auto; margin-right: auto; }
  .figure-portrait figcaption { text-align: center; }
  .bio-photo { width: 100%; aspect-ratio: 1 / 1; object-fit: cover; border: 1px solid var(--border); display: block; }

  /* ── Price tag ── */
  .price-tag {
    display: inline-flex; align-items: center; gap: 8px;
    background: var(--accent); color: #1A1A1A;
    font-size: 11px; font-weight: 600; letter-spacing: .08em;
    text-transform: uppercase; padding: 6px 14px;
    margin-bottom: 28px;
  }
  .price-tag s { color: #8a5a08; text-decoration-color: #8a5a08; }

  /* ── Stats strip ── */
  .stats { display: flex; flex-wrap: wrap; gap: 32px; padding-top: 36px; margin-top: 36px; border-top: 1px solid var(--border); }
  .stat-val { font-size: 24px; font-weight: 600; color: var(--ink); display: block; }
  .stat-lbl { font-size: 12px; color: var(--muted); margin-top: 2px; display: block; }

  /* ── Divider rows ── */
  .divide > * + * { border-top: 1px solid var(--border2); }

  /* ── Pain rows ── */
  .pain-row { display: grid; grid-template-columns: 160px 1fr; gap: 16px; padding: 24px 0; align-items: start; }
  @media (max-width: 600px) { .pain-row { grid-template-columns: 1fr; gap: 6px; } }
  .pain-label { font-size: 11px; letter-spacing: .07em; text-transform: uppercase; color: var(--accent-dark); font-weight: 600; padding-top: 2px; }
  .pain-body { font-size: 15px; color: var(--ink2); line-height: 1.65; }
  .pain-note { font-size: 13px; color: var(--muted); font-style: italic; margin-top: 6px; }

  /* ── Dark callout ── */
  .callout-dark { background: var(--ink); color: var(--bg); padding: 28px 32px; margin: 28px 0; }
  .callout-dark { border-left: 4px solid var(--accent); }
  .callout-dark p { font-size: 16px; line-height: 1.6; }
  .callout-dark span { color: var(--accent); font-weight: 500; }

  /* ── Timeline ── */
  .timeline { border: 1px solid var(--border); }
  .tl-head { display: grid; grid-template-columns: 100px 1fr 1fr; gap: 12px; padding: 10px 20px; background: var(--bg); border-bottom: 1px solid var(--border2); }
  .tl-head span { font-size: 11px; letter-spacing: .07em; text-transform: uppercase; color: var(--muted); font-weight: 500; }
  .tl-row { display: grid; grid-template-columns: 100px 1fr 1fr; gap: 12px; padding: 16px 20px; border-top: 1px solid var(--border2); align-items: start; }
  @media (max-width: 580px) {
    .tl-head { grid-template-columns: 1fr 1fr; }
    .tl-head span:nth-child(2) { display: none; }
    .tl-row { grid-template-columns: 80px 1fr; }
    .tl-row .tl-detail { display: none; }
  }
  .tl-period { font-size: 13px; font-weight: 600; color: var(--ink); }
  .tl-detail { font-size: 13px; color: var(--muted); }
  .tl-result { font-size: 13px; font-weight: 600; color: var(--accent-dark); }

  /* ── Case grid ── */
  .case-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); gap: 8px; margin-top: 24px; }
  .case-card { background: var(--bg); border: 1px solid var(--border); padding: 14px 16px; }
  .case-biz  { font-size: 13px; font-weight: 500; color: var(--ink); }
  .case-city { font-size: 11px; color: var(--muted); margin-top: 2px; }

  /* ── Three column grid ── */
  .three-col { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; background: var(--border2); border: 1px solid var(--border2); margin-top: 28px; }
  @media (max-width: 640px) { .three-col { grid-template-columns: 1fr; } }
  .three-col-cell { background: var(--white); padding: 24px 22px; }
  .cell-head { font-size: 11px; font-weight: 500; letter-spacing: .07em; text-transform: uppercase; color: var(--muted); margin-bottom: 10px; }
  .cell-body { font-size: 13px; color: var(--ink2); line-height: 1.65; }

  /* ── Bio layout ── */
  .bio-grid { display: grid; grid-template-columns: 1fr 260px; gap: 40px; align-items: start; }
  @media (max-width: 640px) { .bio-grid { grid-template-columns: 1fr; } }
  .bio-sidebar > * + * { margin-top: 10px; }
  .sidebar-card { border: 1px solid var(--border); background: var(--bg); padding: 20px; }
  .sidebar-dark { background: var(--ink); color: var(--bg); padding: 20px; }
  .sidebar-dark .cell-head { color: #BCBAB5; }
  .sidebar-dark .cell-body { color: #D0CEC9; font-size: 13px; line-height: 1.65; font-style: italic; }
  .cred-list { list-style: none; }
  .cred-list li { display: flex; gap: 8px; font-size: 13px; color: var(--ink2); padding: 4px 0; }
  .cred-list li::before { content: '—'; color: var(--muted); flex-shrink: 0; }

  /* ── Checklist ── */
  .checklist { border: 1px solid var(--border); background: var(--white); }
  .check-item { display: flex; gap: 16px; align-items: flex-start; padding: 14px 20px; border-top: 1px solid var(--border2); }
  .check-item:first-child { border-top: none; }
  .check-icon { flex-shrink: 0; margin-top: 2px; }
  .check-icon svg { width: 16px; height: 16px; stroke: var(--accent-dark); }
  .check-text { font-size: 14px; color: var(--ink2); line-height: 1.55; }

  /* ── Module grid ── */
  .module-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1px; background: var(--border2); border: 1px solid var(--border2); }
  @media (max-width: 580px) { .module-grid { grid-template-columns: 1fr; } }
  .module-cell { background: var(--white); padding: 22px 24px; }
  .mod-num { font-family: 'DM Mono', monospace; font-size: 12px; font-weight: 500; color: var(--accent-dark); margin-bottom: 10px; }
  .mod-title { font-size: 14px; font-weight: 600; color: var(--ink); margin-bottom: 6px; }
  .mod-desc { font-size: 13px; color: var(--muted); line-height: 1.6; }

  /* ── Bonus items ── */
  .bonus-list > * + * { margin-top: 8px; }
  .bonus-item { display: flex; gap: 16px; align-items: flex-start; background: var(--bg); border: 1px solid var(--border); padding: 18px 20px; }
  .bonus-body { flex: 1; }
  .bonus-title { font-size: 14px; font-weight: 600; color: var(--ink); margin-bottom: 4px; }
  .bonus-desc { font-size: 13px; color: var(--muted); line-height: 1.55; }
  .bonus-val { text-align: right; flex-shrink: 0; }
  .bonus-val .tiny { display: block; }
  .bonus-val strong { font-size: 14px; font-weight: 600; color: var(--accent-dark); }

  /* ── Pricing ── */
  .pricing-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; align-items: start; }
  @media (max-width: 620px) { .pricing-grid { grid-template-columns: 1fr; } }
  .value-table { border: 1px solid var(--border); background: var(--white); }
  .value-table-head { padding: 14px 20px; border-bottom: 1px solid var(--border2); }
  .value-row { display: flex; justify-content: space-between; align-items: center; padding: 13px 20px; border-top: 1px solid var(--border2); font-size: 13px; }
  .value-row:first-child { border-top: none; }
  .value-row.total { background: var(--bg); font-weight: 600; }
  .value-row span:last-child { color: var(--muted); }
  .value-row.total span:last-child { color: var(--ink); font-weight: 600; }

  .cta-block { background: var(--ink); color: var(--bg); padding: 28px; border-top: 3px solid var(--accent); }
  .cta-price-row { display: flex; align-items: baseline; gap: 12px; margin: 10px 0 6px; }
  .cta-price { font-size: 52px; font-weight: 600; line-height: 1; color: var(--accent); }
  .cta-was { font-size: 18px; color: #777; text-decoration: line-through; text-decoration-color: #666; }
  .cta-sub { font-size: 13px; color: #BCBAB5; margin-bottom: 24px; }
  .btn-light { display: block; width: 100%; background: var(--bg); color: var(--ink); padding: 16px; font-size: 14px; font-weight: 700; letter-spacing: .02em; text-align: center; text-decoration: none; cursor: pointer; transition: background .15s; border: none; border-radius: 10px; }
  .btn-light:hover { background: #fff; }
  .cta-note { font-size: 11px; color: #888; text-align: center; margin-top: 10px; }

  .guarantee { border: 1px solid var(--border); border-left: 4px solid var(--accent); background: var(--white); padding: 20px; margin-top: 10px; }
  .guarantee .cell-head { margin-bottom: 8px; }

  /* ── Business tags ── */
  .biz-tags { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 14px; }
  .biz-tag { font-size: 12px; color: var(--ink3); border: 1px solid var(--border); background: var(--white); padding: 5px 12px; }

  /* ── Section padding ── */
  /* longhand de propósito: .wrap controla o padding lateral e .pad o vertical
     no mesmo elemento — shorthand `padding` em qualquer um anula o outro */
  .pad { padding-top: 64px; padding-bottom: 64px; }
  @media (max-width: 600px) { .pad { padding-top: 48px; padding-bottom: 48px; } }

  /* ── Body copy spacing ── */
  .prose > * + * { margin-top: 18px; }

  /* ── Mobile breathing room ── */
  @media (max-width: 600px) {
    .wrap { padding-left: 22px; padding-right: 22px; }
    .h1 br, .h2 br, .lead br { display: none; }
    .callout-dark { padding: 22px 22px; }
    .cta-block, .cta-price { word-break: break-word; }
    .cta-price { font-size: 46px; }
    .btn, .btn-sm { display: block; width: 100%; text-align: center; padding: 17px 20px; margin: 4px 0 14px; }
    .btn + .tiny { text-align: center; }
    .link-free { display: block; width: 100%; text-align: center; margin-top: 10px; }
  }

  /* ── Boost popup ── */
  #bp-overlay { position: fixed; inset: 0; z-index: 9990; background: rgba(26,26,26,.55); display: none; align-items: center; justify-content: center; padding: 20px; }
  #bp-overlay.open { display: flex; }
  .bp-modal { background: var(--white); border: 1px solid var(--border); border-top: 4px solid var(--accent); border-radius: 16px; max-width: 460px; width: 100%; padding: 30px 26px 24px; position: relative; box-shadow: 0 30px 80px -30px rgba(0,0,0,.5); animation: bpIn .3s ease; }
  @keyframes bpIn { from { opacity: 0; transform: translateY(12px); } to { opacity: 1; transform: none; } }
  .bp-close { position: absolute; top: 10px; right: 10px; background: none; border: none; font-size: 15px; color: var(--muted); cursor: pointer; padding: 6px; }
  .bp-h { font-size: 23px; font-weight: 600; letter-spacing: -.02em; line-height: 1.18; margin-bottom: 12px; color: var(--ink); }
  .bp-p { font-size: 14.5px; line-height: 1.65; color: var(--ink3); margin-bottom: 18px; }
  .bp-p strong { color: var(--ink); }
  .bp-sub { font-size: 11.5px; color: var(--muted); text-align: center; margin-top: 10px; }
  .bp-later { display: block; margin: 12px auto 0; background: none; border: none; font-family: inherit; font-size: 12.5px; color: var(--muted); cursor: pointer; text-decoration: underline; text-underline-offset: 3px; }
</style>
</head>
<body>
<!-- Google Tag Manager (noscript) -->
<noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-KL573474"
height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
<!-- End Google Tag Manager (noscript) -->

<!-- HERO -->
<section class="s-bg" style="background:linear-gradient(170deg,#FFF1DA 0%,#FBF0E0 30%,var(--bg) 75%); position:relative;">
  <div class="lang-switch"><a href="/local-ads" data-setlang="en" title="English">🇬🇧</a><span class="active" title="Deutsch">🇩🇪</span></div>
  <div class="wrap pad">
    <div class="price-tag">Aktionspreis — 27 € &nbsp;<s>197 €</s></div>
    <p class="tiny" style="margin-top:-16px; margin-bottom:24px;">Einführungspreis für die ersten Teilnehmer. Er steigt auf 197 €, sobald die ersten Bewertungen da sind.</p>
    <h1 class="h1">Hör auf, mit „Boosten“ Geld <br>an Facebook &amp; Instagram zu spenden!</h1>
    <p class="lead">Der komplette Blueprint, um Anzeigen selbst zu erstellen und zu schalten — von deiner allerersten Kampagne bis zu deinen ersten echten Ergebnissen.</p>
    <div class="hero-video" data-hero-video style="margin: 36px 0;">
      <video
        src="/videos/whylabp-cc.mp4"
        poster="/img/launch-ads-mockup.png"
        preload="none"
        playsinline
        controls
        controlsList="nodownload"
        aria-label="Watch This Before You Buy"></video>
      <button type="button" class="hv-play" aria-label="Play video"><span>&#9654; Watch This Before You Buy (4 min)</span></button>
    </div>
    <p class="body" style="max-width:520px; margin-bottom:36px;">Keine Agentur. Kein Freelancer. Kein Raten. Nur ein klares Schritt-für-Schritt-System, dem jeder folgen kann.</p>
    <p class="tiny" style="max-width:520px; margin-top:-20px; margin-bottom:36px; color:var(--muted);">Gebaut von jemandem, der Millionen an Werbebudget für VC-finanzierte Startups verwaltet hat — destilliert in ein System, einfach genug für komplette Anfänger.</p>

    <button type="button" class="btn" data-free-class>Sieh dir die ersten 2 Lektionen an — richte dein Konto mit mir ein →</button>
    <p class="tiny" style="margin-top:14px;">2 komplette Lektionen offen · Keine Karte nötig · Nach Lektion 2 ist dein Meta-Business-Konto live · Kompletter Kurs einmalig 27 €</p>
  </div>
</section>


<!-- SALES VIDEO -->
<section class="s-white">
  <div class="wrap pad">
    <span class="eyebrow">Schau zuerst — 4 Minuten</span>
    <h2 class="h2" style="margin-bottom:24px;">Warum die meisten Anfänger ihre erste Anzeige <br>nie schalten — und wie du es besser machst.</h2>
    <div class="video-frame">
      <iframe
        src="https://www.youtube.com/embed/pqgiUcjp8Zw?controls=0&rel=0&modestbranding=1&showinfo=0&playsinline=1&iv_load_policy=3"
        title="Launch Ads Blueprint"
        loading="lazy"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
        referrerpolicy="strict-origin-when-cross-origin"
        allowfullscreen></iframe>
    </div>
    <p class="video-cap">Davi zeigt dir genau, was drinsteckt und warum das Setup — nicht die Plattform — Anfänger scheitern lässt.</p>
    <div style="margin-top:24px;">
      <button type="button" class="btn btn-sm" data-free-class>Die ersten 2 Lektionen ansehen → (keine Karte nötig)</button>
    </div>
  </div>
</section>


<!-- PAIN -->
<section class="s-bg">
  <div class="wrap pad">
    <span class="eyebrow">Kommt dir das bekannt vor?</span>
    <h2 class="h2">Klingt das nach dir?</h2>

    <div class="divide">

      <div class="pain-row">
        <span class="pain-label">Die Abhängigkeitsfalle</span>
        <div>
          <p class="pain-body">Jedes Mal, wenn du eine Anzeige brauchst, wartest du auf jemand anderen. Einen Freelancer, eine Agentur, „einen Freund, der sich damit auskennt“. Du hast nie die Kontrolle über dein eigenes Marketing.</p>
          <p class="pain-note">Und du bezahlst dafür — jedes einzelne Mal.</p>
        </div>
      </div>

      <div class="pain-row">
        <span class="pain-label">Das einschüchternde Dashboard</span>
        <div>
          <p class="pain-body">Du hast den Werbeanzeigenmanager einmal geöffnet. 14 Menüs, 6 Kampagnenziele und ein Budget-Feld, das du lieber nicht angefasst hast. Du hast den Tab geschlossen.</p>
          <p class="pain-note">„Später“ kam nie.</p>
        </div>
      </div>

      <div class="pain-row">
        <span class="pain-label">Der nutzlose Boost</span>
        <div>
          <p class="pain-body">Du hast „Beitrag bewerben“ gedrückt, 50 € ausgegeben, 200 Likes von Leuten bekommen, die nie etwas kaufen werden — und kein einziges echtes Ergebnis.</p>
          <p class="pain-note">Das war keine Anzeige. Das war eine Spende an Meta.</p>
        </div>
      </div>

      <div class="pain-row">
        <span class="pain-label">Angst, Geld zu verbrennen</span>
        <div>
          <p class="pain-body">Du kennst die Geschichten. „Ich habe 300 € ausgegeben und nichts bekommen.“ Die Vorstellung, dein Budget ohne Ergebnisse versickern zu sehen, hält dich schon vom Anfangen ab.</p>
        </div>
      </div>

      <div class="pain-row">
        <span class="pain-label">Tutorials, die nirgendwo hinführen</span>
        <div>
          <p class="pain-body">Du hast Stunden auf YouTube verbracht. Ein Dutzend „So schaltest du Anzeigen“-Videos gespeichert. Und trotzdem kannst du dich nicht hinsetzen und allein eine Kampagne starten — weil dir niemand die komplette Reihenfolge gezeigt hat, von Anfang bis Ende.</p>
        </div>
      </div>

    </div>
  </div>
</section>


<!-- AGITATION -->
<section class="s-white">
  <div class="wrap pad">
    <span class="eyebrow">Das echte Problem</span>
    <h2 class="h2">Die Plattform ist nicht das Problem. <br>Das Setup ist es.</h2>

    <div class="prose" style="max-width:620px;">
      <p class="body">Öffne den Werbeanzeigenmanager und dich treffen Kampagnen, Anzeigengruppen, der Pixel, Zielgruppen, Budgets, Ziele — tausend Buttons und das Gefühl, dass ein falscher Klick dein Geld verbrennt.</p>
      <p class="body">Also gehst du zum Lernen auf YouTube. Ein Video sagt: Traffic-Kampagnen. Das nächste: Interaktion ist besser. Eines sagt: Interessen targeten, ein anderes: breit gehen. Nach ein paar Stunden bist du <em>verwirrter</em> als am Anfang — und nicht sicher genug, um zu starten.</p>
      <p class="body">Also schiebst du es auf. Du zerdenkst es. Oder du gibst auf und zahlst einer Agentur Hunderte — manchmal Tausende —, damit sie alles für dich einrichtet, bevor du auch nur einen Cent für Anzeigen ausgegeben hast.</p>
    </div>

    <div class="callout-dark" style="max-width:620px;">
      <p>Anfänger scheitern nicht, weil sie nicht klug sind. <span>Sie scheitern, weil ihnen niemand das Setup gezeigt hat — in der richtigen Reihenfolge.</span></p>
    </div>

    <p class="body" style="max-width:620px;">Genau dafür habe ich den Launch Ads Blueprint gebaut: einen praktischen Schritt-für-Schritt-Weg, der dir zeigt, was zu tun ist, wo du klicken musst und wie du startest — ohne Theorie, ohne Jargon, ohne Raten.</p>
  </div>
</section>


<!-- PROOF -->
<section class="s-bg">
  <div class="wrap pad">
    <span class="eyebrow">Echte Ergebnisse</span>
    <h2 class="h2">Ein kompletter Anfänger. <br>Die allererste Kampagne.</h2>
    <p class="body" style="color:var(--muted); margin-bottom:28px;">Startbudget: 8 €/Tag. Nie zuvor den Werbeanzeigenmanager angefasst. Keine Agentur.</p>

    <div class="timeline">
      <div class="tl-head">
        <span>Zeitraum</span>
        <span>Was passiert ist</span>
        <span>Ergebnis</span>
      </div>
      <div class="tl-row">
        <span class="tl-period">Tag 1</span>
        <span class="tl-detail">Setup befolgt: Konto, Pixel, Domain verifiziert</span>
        <span class="tl-result">Erste Kampagne live</span>
      </div>
      <div class="tl-row">
        <span class="tl-period">Woche 1</span>
        <span class="tl-detail">Richtiges Ziel · 8 €/Tag · 3 Anzeigen getestet</span>
        <span class="tl-result">4 echte Leads</span>
      </div>
      <div class="tl-row">
        <span class="tl-period">Woche 2</span>
        <span class="tl-detail">Zahlen gelesen, Verlierer gestoppt, Gewinner skaliert</span>
        <span class="tl-result">11 Leads · je 6,80 €</span>
      </div>
      <div class="tl-row">
        <span class="tl-period">Woche 3</span>
        <span class="tl-detail">Gleiches Setup, ganz ohne Hilfe</span>
        <span class="tl-result">18 Leads · läuft solo</span>
      </div>
    </div>

    <p class="body" style="margin-top:28px; margin-bottom:18px; max-width:560px;">Es geht nicht um die Zahlen. Es geht darum: Jemand, der nie eine Anzeige geschaltet hatte, hat es selbst gemacht — und musste nie jemanden einstellen.</p>

    <div class="case-grid">
      <div class="case-card"><p class="case-biz">Konto eingerichtet</p><p class="case-city">Business Manager + Pixel</p></div>
      <div class="case-card"><p class="case-biz">Ziel gewählt</p><p class="case-city">Das richtige Ziel, mit Absicht</p></div>
      <div class="case-card"><p class="case-biz">Erste Anzeige gebaut</p><p class="case-city">Bild &amp; Video</p></div>
      <div class="case-card"><p class="case-biz">Kennzahlen gelesen</p><p class="case-city">Wusste, was als Nächstes kommt</p></div>
    </div>

    <div style="margin-top:32px;">
      <button type="button" class="btn btn-sm" data-free-class>Die ersten 2 Lektionen ansehen → (keine Karte nötig)</button>
    </div>
  </div>
</section>


<!-- TESTIMONIALS — oculto até haver depoimentos reais -->
<!-- TODO: publish when real testimonials are collected. Never invent testimonials, names, or photos. -->
{SHOW_TESTIMONIALS && (
<section class="s-white">
  <div class="wrap pad">
    <span class="eyebrow">What students say</span>
    <h2 class="h2">From the first cohort.</h2>
    <div class="case-grid" style="grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));">
      <div class="sidebar-card">
        <img class="bio-photo" style="width:48px; height:48px; margin-bottom:12px;" src="/img/PLACEHOLDER-student-1.png" alt="[PLACEHOLDER — real student photo]" loading="lazy" />
        <p class="mod-title">[PLACEHOLDER — Student name]</p>
        <p class="tiny" style="margin-bottom:8px;">[PLACEHOLDER — one-line label, e.g. "First campaign: March 2026"]</p>
        <p class="small">[PLACEHOLDER — 2-sentence quote from a real student. Never invent this.]</p>
      </div>
      <div class="sidebar-card">
        <img class="bio-photo" style="width:48px; height:48px; margin-bottom:12px;" src="/img/PLACEHOLDER-student-2.png" alt="[PLACEHOLDER — real student photo]" loading="lazy" />
        <p class="mod-title">[PLACEHOLDER — Student name]</p>
        <p class="tiny" style="margin-bottom:8px;">[PLACEHOLDER — one-line label]</p>
        <p class="small">[PLACEHOLDER — 2-sentence quote from a real student. Never invent this.]</p>
      </div>
      <div class="sidebar-card">
        <img class="bio-photo" style="width:48px; height:48px; margin-bottom:12px;" src="/img/PLACEHOLDER-student-3.png" alt="[PLACEHOLDER — real student photo]" loading="lazy" />
        <p class="mod-title">[PLACEHOLDER — Student name]</p>
        <p class="tiny" style="margin-bottom:8px;">[PLACEHOLDER — one-line label]</p>
        <p class="small">[PLACEHOLDER — 2-sentence quote from a real student. Never invent this.]</p>
      </div>
    </div>
  </div>
</section>
)}

<!-- MARKET SHIFT -->
<section class="s-white">
  <div class="wrap pad">
    <span class="eyebrow">Der Wandel</span>
    <h2 class="h2">Du brauchst keinen Spezialisten <br>mehr. Du brauchst die Schritte.</h2>

    <div class="prose" style="max-width:620px;">
      <p class="body">Jemanden zu bezahlen, der dein Meta-Ads-Konto korrekt einrichtet, kostet 400, 500, 1.000 € oder mehr — bevor du einen einzigen Euro für Anzeigen ausgegeben hast.</p>
      <p class="body">Gleichzeitig hat Meta selbstverwaltete Anzeigen mächtiger gemacht denn je. KI-gestütztes Targeting. Automatisierte Tests. Smartes Bidding, das optimiert, während du schläfst — <em>wenn</em> dein Setup und Tracking stimmen.</p>
      <p class="body">Früher brauchten die Tools einen Spezialisten. Heute nicht mehr. Das Einzige zwischen dir und einer laufenden Kampagne ist die genaue Reihenfolge.</p>
    </div>

    <div class="three-col">
      <div class="three-col-cell">
        <p class="cell-head">Was es früher brauchte</p>
        <p class="cell-body">Einen zertifizierten Spezialisten, eine Setup-Gebühr von 400–1.000 € oder mehr und Wochen des Wartens.</p>
      </div>
      <div class="three-col-cell">
        <p class="cell-head">Was es heute braucht</p>
        <p class="cell-body">Die richtige Setup-Reihenfolge, das richtige Ziel, funktionierendes Tracking und 5 €/Tag.</p>
      </div>
      <div class="three-col-cell">
        <p class="cell-head">Die einzige Lücke</p>
        <p class="cell-body">Zu wissen, was du konfigurieren musst, in welcher Reihenfolge und warum. Genau das zeigt dir der Blueprint.</p>
      </div>
    </div>

    <figure class="figure figure-portrait">
      <img src="/img/fail-section.png" alt="Laufende Meta-Ads-Kampagnen vom Laptop aus verwalten — überall" loading="lazy" />
      <figcaption>Einmal richtig aufgesetzt, führst und liest du deine Kampagnen selbst — von überall, mit jedem Budget.</figcaption>
    </figure>
  </div>
</section>


<!-- BIO -->
<section class="s-bg">
  <div class="wrap pad">
    <span class="eyebrow">Wer das gebaut hat</span>
    <div class="bio-grid">
      <div>
        <h2 class="h2">Hi. Ich bin Davi Wazlawick.</h2>
        <div class="prose" style="max-width:480px;">
          <p class="body">Ich habe jahrelang Werbekampagnen für Unternehmen jeder Größe erstellt und verwaltet — von kleinen Firmen bis zu Marken mit Millionenbudgets — in Europa und den USA, in Fitness, Gesundheit und B2B-Tech.</p>
          <p class="body">Als VP of Performance bei VAHA, Growth Director bei Pixformance und Berater für Firmen wie Bioniq und WorkMotion habe ich Paid Acquisition mit Budgets von Hunderten bis Millionen aufgebaut — immer mit einem Fokus: Ergebnisse, ohne Geld zu verschwenden.</p>
          <p class="body">Aber die Menschen, die mir im Kopf blieben, waren nicht immer die großen Accounts. Es waren Freunde mit großartigen Ideen und Produkten, die nie verstanden haben, wie sie sie live bringen. Genau für sie habe ich den Launch Ads Blueprint gebaut.</p>
          <p class="body" style="color:var(--ink); font-weight:500;">Das ist kein 40-Stunden-Theoriekurs. Es ist ein praktisches Schritt-für-Schritt-Training, das dir zeigt, was zu tun ist, wo du klickst und wie du startest — selbstsicher.</p>
        </div>
      </div>

      <div class="bio-sidebar">
        <img class="bio-photo" src="/img/davi/davi-headshot.png" alt="Davi Wazlawick" loading="lazy" />
        <div class="sidebar-card">
          <p class="cell-head">Erfahrung</p>
          <ul class="cred-list">
            <li>Jahre Meta Ads gebaut &amp; geschaltet</li>
            <li>Budgets von Hunderten bis Millionen</li>
            <li>Firmen groß und klein, EU &amp; USA</li>
            <li>Komplette Anfänger von null trainiert</li>
          </ul>
          <a href="https://www.linkedin.com/in/davi-wazlawick-43593525/" target="_blank" rel="noopener" class="small" style="display:inline-block; margin-top:12px; color:var(--accent-dark); text-decoration:underline; text-underline-offset:3px;">Mein Profil auf LinkedIn ansehen →</a>
          <!-- TODO: logo strip VAHA / Pixformance / Bioniq / WorkMotion when logo files are available -->
        </div>
        <div class="sidebar-dark">
          <p class="cell-head">Die Regel</p>
          <p class="cell-body">„Ich habe Millionen-Budgets für VC-finanzierte Startups verwaltet. Exakt dieselben Setup-Prinzipien bringen die erste Kampagne eines kompletten Anfängers live — nur im richtigen Maßstab angewendet.“</p>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- PRODUCT -->
<section class="s-white">
  <div class="wrap pad">
    <span class="eyebrow">Das System</span>
    <h2 class="h2">Launch Ads Blueprint</h2>
    <p class="body" style="max-width:520px; margin-bottom:28px;">Ein praktisches Schritt-für-Schritt-Training, das dich von „Ich habe den Werbeanzeigenmanager noch nie geöffnet“ zu deiner ersten laufenden Meta-Ads-Kampagne bringt — richtig gemacht, ohne Raten.</p>

    <div class="checklist" style="max-width:660px;">
      <div class="check-item">
        <span class="check-icon"><svg viewBox="0 0 14 14" fill="none" stroke="#1A1A1A" stroke-width="1.5"><polyline points="1.5,7 5,10.5 12.5,3"/></svg></span>
        <span class="check-text">Bringt deine erste Kampagne in unter 60 Minuten live — auch mit null Erfahrung</span>
      </div>
      <div class="check-item">
        <span class="check-icon"><svg viewBox="0 0 14 14" fill="none" stroke="#1A1A1A" stroke-width="1.5"><polyline points="1.5,7 5,10.5 12.5,3"/></svg></span>
        <span class="check-text">Richtet deinen Business Manager und dein Werbekonto von Anfang an korrekt ein</span>
      </div>
      <div class="check-item">
        <span class="check-icon"><svg viewBox="0 0 14 14" fill="none" stroke="#1A1A1A" stroke-width="1.5"><polyline points="1.5,7 5,10.5 12.5,3"/></svg></span>
        <span class="check-text">Installiert deinen Pixel und verifiziert deine Domain, damit Tracking wirklich funktioniert</span>
      </div>
      <div class="check-item">
        <span class="check-icon"><svg viewBox="0 0 14 14" fill="none" stroke="#1A1A1A" stroke-width="1.5"><polyline points="1.5,7 5,10.5 12.5,3"/></svg></span>
        <span class="check-text">Zeigt dir genau, welches Ziel du wählen solltest — und warum das falsche die falschen Leute anzieht</span>
      </div>
      <div class="check-item">
        <span class="check-icon"><svg viewBox="0 0 14 14" fill="none" stroke="#1A1A1A" stroke-width="1.5"><polyline points="1.5,7 5,10.5 12.5,3"/></svg></span>
        <span class="check-text">Führt dich Schritt für Schritt durch deine erste Anzeige — Bild oder Video</span>
      </div>
      <div class="check-item">
        <span class="check-icon"><svg viewBox="0 0 14 14" fill="none" stroke="#1A1A1A" stroke-width="1.5"><polyline points="1.5,7 5,10.5 12.5,3"/></svg></span>
        <span class="check-text">Gibt dir eine Checkliste vor dem Start, damit du weißt, dass alles stimmt, bevor du veröffentlichst</span>
      </div>
      <div class="check-item">
        <span class="check-icon"><svg viewBox="0 0 14 14" fill="none" stroke="#1A1A1A" stroke-width="1.5"><polyline points="1.5,7 5,10.5 12.5,3"/></svg></span>
        <span class="check-text">Bringt dir bei, deine Zahlen zu lesen — damit du nicht zu früh stoppst oder für Anzeigen zahlst, die du abschalten solltest</span>
      </div>
    </div>

    <div style="margin-top:28px;">
      <button type="button" class="btn btn-sm" data-free-class>Die ersten 2 Lektionen ansehen → (keine Karte nötig)</button>
    </div>
  </div>
</section>


<!-- INCLUDES -->
<section class="s-bg">
  <div class="wrap pad">
    <span class="eyebrow">Was drin ist</span>
    <h2 class="h2">Alles inklusive.</h2>

    <p class="tiny" style="letter-spacing:.07em; text-transform:uppercase; font-weight:500; color:var(--muted); margin-bottom:14px;">6 Kernmodule</p>
    <div class="module-grid">
      <div class="module-cell">
        <p class="mod-num mono">01</p>
        <p class="mod-title">Warum die meisten Anfänger scheitern</p>
        <p class="mod-desc">Der wahre Grund, warum erste Kampagnen floppen — es ist das Setup, nicht die Plattform. Mach das richtig, und alles andere wird leichter.</p>
      </div>
      <div class="module-cell">
        <p class="mod-num mono">02</p>
        <p class="mod-title">Business Manager + Werbekonto-Setup</p>
        <p class="mod-desc">Richte Business Manager und Werbekonto von Tag eins richtig ein — keine chaotischen Reparaturen später.</p>
      </div>
      <div class="module-cell">
        <p class="mod-num mono">03</p>
        <p class="mod-title">Tracking, das wirklich funktioniert</p>
        <p class="mod-desc">Installiere den Meta-Pixel und verifiziere deine Domain, damit Meta optimieren kann — der Schritt, den Anfänger überspringen und teuer bezahlen.</p>
      </div>
      <div class="module-cell">
        <p class="mod-num mono">04</p>
        <p class="mod-title">Das richtige Ziel wählen</p>
        <p class="mod-desc">Wähl das falsche Ziel, und Meta zeigt deine Anzeige den falschen Leuten. So wählst du richtig — und deshalb ist es so wichtig.</p>
      </div>
      <div class="module-cell">
        <p class="mod-num mono">05</p>
        <p class="mod-title">Launch: Kampagne, Budget &amp; erste Anzeige</p>
        <p class="mod-desc">Baue deine erste Kampagne, setze dein Budget und erstelle deine erste Anzeige — Bild oder Video — ohne jeden Klick anzuzweifeln.</p>
      </div>
      <div class="module-cell">
        <p class="mod-num mono">06</p>
        <p class="mod-title">Deine Zahlen lesen</p>
        <p class="mod-desc">Ein Kennzahlen-Guide für Einsteiger: versteh, was die Zahlen bedeuten — damit du weißt, wann du skalierst und wann du stoppst.</p>
      </div>
    </div>

    <p class="tiny" style="letter-spacing:.07em; text-transform:uppercase; font-weight:500; color:var(--muted); margin: 36px 0 14px;">Boni inklusive</p>
    <div class="bonus-list">
      <div class="bonus-item">
        <div class="bonus-body">
          <p class="bonus-title">Meta Ads Launch-Checkliste</p>
          <p class="bonus-desc">Geh jeden Schritt durch, bevor du veröffentlichst — Konto, Pixel, Domain, Ziel, Budget, Creative. Nie wieder fragen, ob dein Setup komplett ist.</p>
        </div>
        <div class="bonus-val"><span class="tiny">Wert</span><strong>47 €</strong></div>
      </div>
      <div class="bonus-item">
        <div class="bonus-body">
          <p class="bonus-title">Kennzahlen-Guide für Einsteiger</p>
          <p class="bonus-desc">Ein Spickzettel in einfacher Sprache für die Zahlen, die zählen — damit du weißt: skalieren, warten oder abschalten.</p>
        </div>
        <div class="bonus-val"><span class="tiny">Wert</span><strong>47 €</strong></div>
      </div>
      <div class="bonus-item">
        <div class="bonus-body">
          <p class="bonus-title">ChatGPT-Prompts für Anzeigentexte</p>
          <p class="bonus-desc">Lückentext-Prompts, die Anzeigentexte in unter 5 Minuten erzeugen — keine Copywriting-Erfahrung nötig.</p>
        </div>
        <div class="bonus-val"><span class="tiny">Wert</span><strong>47 €</strong></div>
      </div>
    </div>
  </div>
</section>


<!-- FAQ -->
<section class="s-bg">
  <div class="wrap pad">
    <span class="eyebrow">Bevor du kaufst</span>
    <h2 class="h2">Fragen, die vor dem Kauf kommen.</h2>
    <div class="divide">
      <div style="padding: 22px 0;">
        <p class="mod-title" style="font-size:15px; margin-bottom:8px;">Wie lange habe ich Zugriff?</p>
        <p class="body">Für immer. Eine Zahlung, lebenslanger Zugriff auf alle 20 Lektionen und jedes künftige Update.</p>
      </div>
      <div style="padding: 22px 0;">
        <p class="mod-title" style="font-size:15px; margin-bottom:8px;">Ich habe noch kein Produkt oder Unternehmen. Bringt mir das nichts?</p>
        <p class="body">Doch — das Setup ist identisch, ob du heute verkaufst oder in sechs Monaten. Du baust Konto, Tracking und Skills einmal auf, und sie sind bereit, wenn du startest. Aber wenn du diese Woche Ergebnisse willst und noch nichts zu bewerben hast, warte, bis du etwas hast.</p>
      </div>
      <div style="padding: 22px 0;">
        <p class="mod-title" style="font-size:15px; margin-bottom:8px;">Funktioniert das in meinem Land?</p>
        <p class="body">Ja. Meta Ads funktioniert überall gleich — gleicher Werbeanzeigenmanager, gleiches Setup, gleiche Prinzipien. Der Kurs ist auf Englisch — einfach und klar, jeder Klick wird am Bildschirm gezeigt.</p>
      </div>
      <div style="padding: 22px 0;">
        <p class="mod-title" style="font-size:15px; margin-bottom:8px;">Wie funktioniert die Rückerstattung?</p>
        <p class="body">Schreib mir innerhalb von 30 Tagen nach dem Kauf. Du bekommst jeden Cent zurück. Keine Formulare, kein „Beweis, dass du es durchgearbeitet hast“, keine Fragen.</p>
      </div>
      <div style="padding: 22px 0;">
        <p class="mod-title" style="font-size:15px; margin-bottom:8px;">Ist das ein Abo?</p>
        <p class="body">Nein. Einmal 27 €. Das war’s.</p>
      </div>
    </div>
  </div>
</section>


<!-- PRICING -->
<section class="s-white" id="pricing">
  <div class="wrap pad">
    <span class="eyebrow">Leg los</span>
    <h2 class="h2">Alles oben. Ein Preis.</h2>

    <div class="pricing-grid">

      <div class="value-table">
        <div class="value-table-head">
          <p class="tiny" style="letter-spacing:.07em; text-transform:uppercase; font-weight:500;">Was du bekommst</p>
        </div>
        <div class="value-row"><span class="small">6 Kernmodule</span><span class="small">197 €</span></div>
        <div class="value-row"><span class="small">Meta Ads Launch-Checkliste</span><span class="small">47 €</span></div>
        <div class="value-row"><span class="small">Kennzahlen-Guide für Einsteiger</span><span class="small">47 €</span></div>
        <div class="value-row"><span class="small">ChatGPT-Prompts</span><span class="small">47 €</span></div>
        <div class="value-row total"><span class="small">Gesamtwert</span><span class="small">338 €</span></div>
      </div>

      <div>
        <div class="cta-block">
          <p class="tiny" style="color:#BCBAB5; letter-spacing:.08em; text-transform:uppercase; font-weight:500;">Preis heute</p>
          <div class="cta-price-row">
            <span class="cta-price">27 €</span>
            <span class="cta-was">197 €</span>
          </div>
          <p class="cta-sub">Einmalzahlung. Kein Abo.</p>
          <p class="tiny" style="color:#BCBAB5; margin-top:-16px; margin-bottom:24px;">Einführungspreis für die ersten Teilnehmer. Er steigt auf 197 €, sobald die ersten Bewertungen da sind.</p>
          <a href={CHECKOUT_URL} target="_blank" rel="noopener" class="btn-light">Ja, gib mir das System →</a>
          <p class="cta-note">Sicherer Checkout über Alfima · Karte &amp; PayPal · Sofortiger Zugriff · 30-Tage-Geld-zurück-Garantie</p>
          <button type="button" class="link-free" data-free-class style="display:block; width:100%; text-align:center; color:#BCBAB5;">Die ersten 2 Lektionen ansehen → (keine Karte nötig)</button>
        </div>

        <div class="guarantee">
          <p class="cell-head">30-Tage-Garantie</p>
          <p class="small" style="color:var(--ink3);">Geh das System durch. Wenn du dich nicht bereit fühlst, deine erste Kampagne zu schalten, schreib mir — ich erstatte jeden Cent. Ohne Fragen.</p>
        </div>
      </div>
    </div>

    <div style="margin-top:56px; padding-top:40px; border-top:1px solid var(--border);">
      <p class="tiny" style="letter-spacing:.08em; text-transform:uppercase; font-weight:500; margin-bottom:14px;">Gemacht für alle, die ihre erste Anzeige schalten</p>
      <div class="biz-tags">
        <span class="biz-tag">Komplette Anfänger</span>
        <span class="biz-tag">Solo-Selbstständige</span>
        <span class="biz-tag">Creator</span>
        <span class="biz-tag">Coaches</span>
        <span class="biz-tag">Online-Shops</span>
        <span class="biz-tag">Freelancer</span>
        <span class="biz-tag">Lokale Unternehmen</span>
        <span class="biz-tag">Nebenprojekte</span>
        <span class="biz-tag">Startups</span>
        <span class="biz-tag">Dienstleister</span>
        <span class="biz-tag">Alle mit einem Produkt</span>
        <span class="biz-tag">Alle mit einer Idee</span>
      </div>
    </div>

    <p class="tiny" style="text-align:center; margin-top:48px;">Launch Ads Blueprint ist ein unabhängiger Kurs. Ergebnisse hängen von deinem Angebot, Markt und deiner Umsetzung ab.</p>
  </div>
</section>


<!-- SOCIAL PROOF WIDGET -->
<style>
  #sp-widget {
    position: fixed;
    bottom: 24px;
    left: 24px;
    z-index: 9999;
    width: 296px;
    background: #fff;
    border: 1px solid #E0DDD8;
    box-shadow: 0 8px 32px -8px rgba(0,0,0,.18), 0 2px 8px -4px rgba(0,0,0,.08);
    font-family: 'DM Sans', system-ui, sans-serif;
    transform: translateX(calc(-100% - 32px));
    transition: transform .55s cubic-bezier(.34,1.56,.64,1);
    pointer-events: auto;
  }
  #sp-widget.sp-in  { transform: translateX(0); }
  #sp-widget.sp-out { transform: translateX(calc(-100% - 32px)); transition: transform .38s cubic-bezier(.4,0,.2,1); }
  .sp-body { display: flex; gap: 12px; align-items: flex-start; padding: 14px 14px 12px; }
  .sp-avatar {
    width: 38px; height: 38px; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 15px; font-weight: 700; color: #fff; flex-shrink: 0; margin-top: 1px;
  }
  .sp-copy { flex: 1; min-width: 0; }
  .sp-name { font-size: 13px; font-weight: 600; color: #1A1A1A; margin-bottom: 2px; }
  .sp-action { font-size: 12px; color: #444; line-height: 1.45; }
  .sp-action strong { color: #1A1A1A; font-weight: 600; }
  .sp-meta { font-size: 11px; color: #999; margin-top: 5px; }
  .sp-close {
    background: none; border: none; cursor: pointer;
    color: #C0BDB8; font-size: 13px; line-height: 1; padding: 0;
    flex-shrink: 0; margin-top: -1px; transition: color .15s;
  }
  .sp-close:hover { color: #555; }
  .sp-track { height: 3px; background: #F0EDEA; }
  .sp-bar   { height: 3px; width: 100%; }
  @media (max-width: 400px) { #sp-widget { width: calc(100vw - 32px); left: 16px; bottom: 16px; } }
</style>

<div id="sp-widget" aria-live="polite" aria-atomic="true" role="status">
  <div class="sp-body">
    <div class="sp-avatar" id="sp-av"></div>
    <div class="sp-copy">
      <div class="sp-name"  id="sp-name"></div>
      <div class="sp-action" id="sp-action"></div>
      <div class="sp-meta"  id="sp-meta"></div>
    </div>
    <button class="sp-close" id="sp-close" aria-label="Close">✕</button>
  </div>
  <div class="sp-track"><div class="sp-bar" id="sp-bar"></div></div>
</div>

<script>
(function () {
  var names   = ['James T.','Sarah K.','Michael B.','Emily R.','David L.','Jessica M.','Chris W.','Amanda H.'];
  var actions = ['hat gerade gekauft:','hat sich gerade angemeldet für:','hat gerade upgegradet auf:'];
  var cities  = ['New York 🇺🇸','Los Angeles 🇺🇸','London 🇬🇧','Toronto 🇨🇦','Sydney 🇦🇺','Berlin 🇩🇪'];
  var times   = ['gerade eben','vor 2 Min.','vor 4 Min.','vor 9 Min.','vor 14 Min.','vor 22 Min.'];
  var colors  = ['#F5A623','#4A7FD4','#7B68EE','#2DB5A3','#E8697A','#5ABF8A'];
  var product = 'Launch Ads Blueprint';

  var w      = document.getElementById('sp-widget');
  var avEl   = document.getElementById('sp-av');
  var nameEl = document.getElementById('sp-name');
  var actEl  = document.getElementById('sp-action');
  var metaEl = document.getElementById('sp-meta');
  var barEl  = document.getElementById('sp-bar');
  var closeBtn = document.getElementById('sp-close');

  var VISIBLE   = 5000;
  var MAX_SHOWS = 4;
  var shows     = 0;
  var visible   = false;
  var autoHide  = null;

  function pick(arr) { return arr[Math.floor(Math.random() * arr.length)]; }

  function show() {
    var name  = pick(names);
    var color = pick(colors);

    avEl.textContent        = name[0];
    avEl.style.background   = color;
    nameEl.textContent      = name;
    actEl.innerHTML         = pick(actions) + ' <strong>' + product + '</strong>';
    metaEl.textContent      = pick(times) + ' · ' + pick(cities);
    barEl.style.background  = color;

    // reset bar without transition, then animate
    barEl.style.transition  = 'none';
    barEl.style.width       = '100%';

    w.classList.remove('sp-out');
    // force reflow so transition fires
    void w.offsetWidth;
    w.classList.add('sp-in');
    visible = true;

    requestAnimationFrame(function () {
      requestAnimationFrame(function () {
        barEl.style.transition = 'width ' + VISIBLE + 'ms linear';
        barEl.style.width = '0%';
      });
    });

    autoHide = setTimeout(hide, VISIBLE);
  }

  function hide() {
    if (!visible) return;
    clearTimeout(autoHide);
    w.classList.remove('sp-in');
    w.classList.add('sp-out');
    visible = false;
  }

  closeBtn.addEventListener('click', hide);

  // primeira aparição após 15 s; depois intervalos aleatórios de 40–90 s, máx. 4 por visita
  setTimeout(function loop() {
    if (shows++ >= MAX_SHOWS) return;
    show(); // esconde sozinho após VISIBLE
    setTimeout(loop, VISIBLE + 40000 + Math.random() * 50000);
  }, 15000);
}());
</script>

<!-- FREE FIRST LESSON MODAL -->
<div id="fc-overlay" role="dialog" aria-modal="true" aria-labelledby="fc-title">
  <div class="fc-modal">
    <button type="button" class="fc-close" id="fc-close" aria-label="Close">✕</button>

    <!-- State 1: capture -->
    <div id="fc-form-state">
      <span class="eyebrow">Kostenlose Vorschau</span>
      <h3 id="fc-title" class="h2" style="font-size:24px; margin-bottom:10px;">Zwei komplette Lektionen. Offen.</h3>
      <p class="small" style="margin-bottom:22px;">Lektion 1 zeigt dir, warum die meisten Anfänger scheitern. In Lektion 2 richten wir gemeinsam dein Meta-Business-Konto ein — Schritt für Schritt. Schau beide, mach das Setup, entscheide dann. Sag mir nur, wer du bist. (Die Lektionen sind auf Englisch.)</p>
      <form id="fc-form" novalidate>
        <div class="fc-field">
          <label for="fc-name">Dein Name</label>
          <input id="fc-name" name="fullName" type="text" autocomplete="name" placeholder="Jane Smith" required />
        </div>
        <div class="fc-field">
          <label for="fc-email">Deine E-Mail</label>
          <input id="fc-email" name="email" type="email" autocomplete="email" placeholder="jane@example.com" required />
        </div>
        <button type="submit" class="btn" id="fc-submit" style="display:block; width:100%; text-align:center;">Lektionen 1 &amp; 2 öffnen →</button>
        <p class="fc-error" id="fc-error">Bitte gib deinen Namen und eine gültige E-Mail an.</p>
        <p class="tiny" style="margin-top:12px;">Ich schicke dir den Lektions-Link auch per E-Mail, plus gelegentliche Ads-Tipps. Jederzeit abbestellbar. <a href="/privacy-policy" style="text-decoration:underline;">Datenschutzerklärung</a>.</p>
      </form>
    </div>

    <!-- State 2: video -->
    <div id="fc-video-state" style="display:none;">
      <span class="eyebrow" id="fc-eyebrow">Lektionen 1 &amp; 2 von 20 — offene Vorschau</span>
      <h3 class="h2" id="fc-lesson-title" style="font-size:24px; margin-bottom:16px;">Why most beginners fail.</h3>
      <div class="fc-video">
        <video id="fc-player" src="/videos/lesson-1.mp4" controls controlsList="nodownload" preload="none" playsinline></video>
      </div>
      <a href={CHECKOUT_URL} target="_blank" rel="noopener" class="btn" id="fc-next-cta" data-mode="free" style="display:block; text-align:center; margin-top:16px;">Nächste Lektion → Lektion 2 ansehen (weiter kostenlos)</a>
      <p class="tiny" style="text-align:center; margin-top:8px;">Sicherer Checkout über Alfima · Karte &amp; PayPal · Sofortiger Zugriff · 30-Tage-Geld-zurück-Garantie</p>
      <div class="fc-lessons">
        <div class="fc-lesson fc-playable" data-lesson="0"><span class="mono">01</span> Why most beginners fail <span class="fc-tag">▶ Läuft gerade</span></div>
        <div class="fc-lesson fc-playable" data-lesson="1"><span class="mono">02</span> Who the Meta Ads course is for <span class="fc-tag">▶ Kostenlos ansehen</span></div>
        <div class="fc-lesson locked"><span class="mono">03</span> Meta Ads setup checklist <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">04</span> Creating the Facebook page <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">05</span> Adding Ads Manager and payment <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">06</span> Connecting Instagram to your Facebook ads account <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">07</span> Meta Events setup for ad tracking <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">08</span> How to install the Meta Pixel and test events <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">09</span> Meta Ads campaign goals explained <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">10</span> Setting up a Meta lead campaign ad set <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">11</span> Setting up a Meta lead campaign ad set — part 2 <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">12</span> Master Facebook Ads audiences and targeting <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">13</span> Meta Advantage+ targeting controls explained <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">14</span> Facebook Ads setup: excluding key countries <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">15</span> Building your first ad: image <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">16</span> Building your first ad: video <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">17</span> Final review before you hit publish <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">18</span> Wrapping up ads with some tips <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">19</span> Setting up events from Meta to optimise <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="mono">20</span> Testing the event and final class <span class="fc-tag">🔒</span></div>
        <div class="fc-lock-note" id="fc-lock-note">
          <p id="fc-lock-text"></p>
          <a href={CHECKOUT_URL} target="_blank" rel="noopener" class="btn btn-sm" style="display:block; text-align:center;">Kompletten Kurs freischalten — 27 € →</a>
        </div>
      </div>
      <div class="cta-block" id="fc-offer" style="margin-top:20px; padding:22px 22px 18px;">
        <p class="tiny" style="color:#BCBAB5; letter-spacing:.08em; text-transform:uppercase; font-weight:500;">Bereit für Lektion 3?</p>
        <div class="cta-price-row" style="margin:8px 0 4px;">
          <span class="cta-price" style="font-size:38px;">€27</span>
          <span class="cta-was" style="font-size:15px;">€197</span>
        </div>
        <p class="cta-sub" style="margin-bottom:10px;">Als Nächstes: Tracking. Die Pixel-Installation entscheidet, ob Meta deine Anzeigen überhaupt optimieren kann — und genau hier geben die meisten auf. Ich führe dich Klick für Klick hindurch.</p>
        <p class="cta-sub" style="margin-bottom:16px;">Einmalzahlung · Schaltet alle 18 übrigen Lektionen + Boni frei — sofort auf der Alfima-Plattform</p>
        <a href={CHECKOUT_URL} target="_blank" rel="noopener" class="btn-light" id="fc-next" style="text-align:center;">Nächste Lektion → Kaufen &amp; auf Alfima weiterschauen</a>
        <p class="cta-note">Sicherer Checkout über Alfima · Karte &amp; PayPal · Sofortiger Zugriff · 30-Tage-Geld-zurück-Garantie</p>
      </div>
    </div>
  </div>
</div>

<!-- BOOST POPUP -->
<div id="bp-overlay" role="dialog" aria-modal="true" aria-labelledby="bp-h">
  <div class="bp-modal">
    <button type="button" class="bp-close" id="bp-close" aria-label="Close">✕</button>
    <span class="eyebrow">Kurze Frage</span>
    <h3 class="bp-h" id="bp-h">Schon mal „Boosten“ gedrückt — und nichts bekommen?</h3>
    <p class="bp-p">50 €. 200 Likes. Null neue Kunden. Das war keine Anzeige — <strong>das war eine Spende an Meta</strong>. Es gibt ein Basis-Setup, das aus Boosts echte Kampagnen macht — und die ersten 2 Lektionen sind offen.</p>
    <button type="button" class="btn" id="bp-cta" data-free-class style="display:flex; width:100%; justify-content:center; text-align:center;">Die ersten 2 Lektionen kostenlos ansehen →</button>
    <p class="bp-sub">Keine Karte nötig · Nach Lektion 2 ist dein Meta-Business-Konto live</p>
    <button type="button" class="bp-later" id="bp-later">Vielleicht später</button>
  </div>
</div>

<script>
(function () {
  var UNLOCK_KEY = "gr_free_class_v1";

  var overlay    = document.getElementById("fc-overlay");
  var formState  = document.getElementById("fc-form-state");
  var videoState = document.getElementById("fc-video-state");
  var form       = document.getElementById("fc-form");
  var submitBtn  = document.getElementById("fc-submit");
  var errorEl    = document.getElementById("fc-error");
  var player     = document.getElementById("fc-player");

  // ── Helpers (mesmo padrão do FormScript do site principal) ─────────
  async function sha256(value) {
    var normalized = (value || "").toLowerCase().trim();
    if (!normalized) return "";
    var buffer = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(normalized));
    return Array.from(new Uint8Array(buffer)).map(function (b) { return b.toString(16).padStart(2, "0"); }).join("");
  }
  function getCookie(name) {
    var m = document.cookie.match("(^|;)\\s*" + name + "\\s*=\\s*([^;]+)");
    return m ? m.pop() : "";
  }
  function getFbc() {
    var fbc = getCookie("_fbc");
    if (fbc) return fbc;
    var m = window.location.search.match(/[?&]fbclid=([^&]+)/);
    return m ? "fb.1." + Date.now() + "." + decodeURIComponent(m[1]) : "";
  }
  function getUrlParams() {
    var params = {};
    new URLSearchParams(window.location.search).forEach(function (v, k) { if (v) params[k] = v; });
    return params;
  }

  function isUnlocked() {
    try { return !!localStorage.getItem(UNLOCK_KEY); } catch (e) { return false; }
  }
  function renderState() {
    var unlocked = isUnlocked();
    formState.style.display  = unlocked ? "none" : "block";
    videoState.style.display = unlocked ? "block" : "none";
  }
  function openModal() {
    renderState();
    overlay.classList.add("open");
    document.body.style.overflow = "hidden";
  }
  function closeModal() {
    overlay.classList.remove("open");
    document.body.style.overflow = "";
    if (player) player.pause();
  }

  // ── Open/close wiring ───────────────────────────────────────────────
  var triggers = document.querySelectorAll("[data-free-class]");
  for (var i = 0; i < triggers.length; i++) {
    triggers[i].addEventListener("click", function (e) { e.preventDefault(); openModal(); });
  }
  document.getElementById("fc-close").addEventListener("click", closeModal);
  overlay.addEventListener("click", function (e) { if (e.target === overlay) closeModal(); });
  document.addEventListener("keydown", function (e) {
    if (e.key === "Escape" && overlay.classList.contains("open")) closeModal();
  });

  // ── Submit ──────────────────────────────────────────────────────────
  form.addEventListener("submit", async function (e) {
    e.preventDefault();
    var fullName = document.getElementById("fc-name").value.trim();
    var email    = document.getElementById("fc-email").value.trim().toLowerCase();

    if (!fullName || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
      errorEl.textContent = "Bitte gib deinen Namen und eine gültige E-Mail an.";
      errorEl.style.display = "block";
      return;
    }
    errorEl.style.display = "none";
    submitBtn.disabled = true;
    var originalLabel = submitBtn.textContent;
    submitBtn.textContent = "Wird geöffnet…";

    var nameParts = fullName.split(/\s+/);
    var eventId   = Date.now() + "_" + Math.floor(Math.random() * 1e15);
    var emHash = await sha256(email);
    var fnHash = await sha256(nameParts[0]);
    var lnHash = await sha256(nameParts.slice(1).join(" "));
    var fbp = getCookie("_fbp");
    var fbc = getFbc();

    try {
      var res = await fetch("/api/leads", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          leadType: "free_class",
          product:  "launch-ads-blueprint",
          fullName: fullName,
          email:    email,
          meta_event_id:         eventId,
          meta_em:               emHash,
          meta_fn:               fnHash,
          meta_ln:               lnHash,
          meta_fbp:              fbp,
          meta_fbc:              fbc,
          meta_user_agent:       navigator.userAgent,
          meta_event_source_url: window.location.href,
          submitted_at:          new Date().toISOString(),
          marketing_consent:     !!window.__marketingConsent,
          url_params:            getUrlParams(),
        }),
      });
      if (!res.ok) throw new Error("bad status");

      try { localStorage.setItem(UNLOCK_KEY, JSON.stringify({ email: email, at: new Date().toISOString() })); } catch (err) {}

      // Lead: pixel gated por consentimento + dataLayer para o GTM
      if (typeof window.trackMarketing === "function") {
        window.trackMarketing("Lead", { content_name: "Free Lesson 1", content_category: "free_class" }, { eventID: eventId });
      }
      window.dataLayer = window.dataLayer || [];
      window.dataLayer.push({
        event: "free_class_lead",
        event_id: eventId,
        em: emHash, fn: fnHash, ln: lnHash,
        fbp: fbp, fbc: fbc,
      });

      renderState();
      if (player && player.play) player.play().catch(function () {});
    } catch (err) {
      errorEl.textContent = "Etwas ist schiefgelaufen. Bitte versuch es erneut.";
      errorEl.style.display = "block";
    } finally {
      submitBtn.disabled = false;
      submitBtn.textContent = originalLabel;
    }
  });

  // ── Free lessons: player com aulas 1 e 2 ────────────────────────────
  var LESSONS = [
    { label: "Lektion 1 von 20 — offene Vorschau", title: "Why most beginners fail.",        src: "/videos/lesson-1.mp4" },
    { label: "Lektion 2 von 20 — offene Vorschau", title: "Who the Meta Ads course is for.", src: "/videos/lesson-2.mp4" },
  ];
  var currentLesson = 0;
  var milestones = { 25: false, 50: false, 75: false, 95: false };

  function playLesson(i, autoplay) {
    currentLesson = i;
    milestones = { 25: false, 50: false, 75: false, 95: false };
    document.getElementById("fc-eyebrow").textContent = LESSONS[i].label;
    document.getElementById("fc-lesson-title").textContent = LESSONS[i].title;
    // CTA abaixo do vídeo: na aula 1 leva à aula 2 (grátis); na aula 2 vira compra
    var nextCta = document.getElementById("fc-next-cta");
    if (nextCta) {
      nextCta.dataset.mode = i === 0 ? "free" : "paid";
      nextCta.textContent = i === 0
        ? "Nächste Lektion → Lektion 2 ansehen (weiter kostenlos)"
        : "Nächste Lektion → Kaufen & weiterschauen — 27 €";
    }
    var rows = document.querySelectorAll(".fc-lesson.fc-playable");
    for (var r = 0; r < rows.length; r++) {
      var active = Number(rows[r].dataset.lesson) === i;
      rows[r].classList.toggle("active", active);
      rows[r].querySelector(".fc-tag").textContent = active ? "▶ Läuft gerade" : "▶ Kostenlos ansehen";
    }
    if (player.getAttribute("src") !== LESSONS[i].src) player.setAttribute("src", LESSONS[i].src);
    if (autoplay && player.play) player.play().catch(function () {});
  }

  document.querySelectorAll(".fc-lesson.fc-playable").forEach(function (row) {
    row.addEventListener("click", function () { playLesson(Number(row.dataset.lesson), true); });
  });

  var nextCtaEl = document.getElementById("fc-next-cta");
  if (nextCtaEl) {
    nextCtaEl.addEventListener("click", function (e) {
      if (nextCtaEl.dataset.mode === "free") { e.preventDefault(); playLesson(1, true); }
    });
  }

  // ── Aulas bloqueadas: clique mostra aviso + CTA de compra ────────────
  var lockNote = document.getElementById("fc-lock-note");
  document.querySelectorAll(".fc-lesson.locked").forEach(function (row) {
    row.addEventListener("click", function () {
      var num = row.querySelector(".mono").textContent;
      var title = Array.prototype.filter
        .call(row.childNodes, function (n) { return n.nodeType === 3; })
        .map(function (n) { return n.textContent; }).join("").trim();
      document.getElementById("fc-lock-text").innerHTML =
        "Lektion " + num + " — <strong>" + title + "</strong> — ist Teil des vollständigen Kurses. " +
        "Schalte alle 18 übrigen Lektionen + Boni frei — einmalig <strong>27 €</strong> <s>197 €</s>.";
      row.after(lockNote);
      lockNote.style.display = "block";
      window.dataLayer = window.dataLayer || [];
      window.dataLayer.push({ event: "free_class_locked_click", lesson: Number(num) });
    });
  });

  // ── Video progress → dataLayer (25/50/75/95%) ───────────────────────
  if (player) {
    player.addEventListener("ended", function () {
      // fim da aula 1 → emenda na aula 2; fim da aula 2 → destaca a oferta
      if (currentLesson === 0) { playLesson(1, true); return; }
      var offer = document.getElementById("fc-offer");
      if (!offer) return;
      offer.scrollIntoView({ behavior: "smooth", block: "center" });
      offer.classList.add("fc-attn");
      setTimeout(function () { offer.classList.remove("fc-attn"); }, 4000);
    });
    player.addEventListener("timeupdate", function () {
      if (!player.duration) return;
      var pct = (player.currentTime / player.duration) * 100;
      for (var mark in milestones) {
        if (!milestones[mark] && pct >= Number(mark)) {
          milestones[mark] = true;
          window.dataLayer = window.dataLayer || [];
          window.dataLayer.push({ event: "free_class_progress", lesson: currentLesson + 1, video_percent: Number(mark) });
          if (mark === "50" && typeof window.trackMarketing === "function") {
            window.trackMarketing("ViewContent", { content_name: "Free Lesson " + (currentLesson + 1), content_category: "free_class" });
          }
        }
      }
    });
  }
})();
</script>
<!-- End free first lesson modal -->

<!-- Cookie Banner -->
<div id="cookie-banner" style="position:fixed; left:16px; right:16px; bottom:16px; z-index:9999; display:none; max-width:560px; margin:0 auto; background:var(--white); color:var(--ink2); border:1px solid #E5E1DB; border-radius:14px; box-shadow:0 12px 40px rgba(0,0,0,.12); padding:18px 20px;">
  <p style="font-size:13px; line-height:1.5; margin-bottom:14px; color:var(--ink2);">
    Wir verwenden Cookies, um deine Erfahrung zu verbessern und unser Marketing zu messen. Mehr dazu in unserer
    <a href="/privacy-policy" style="color:var(--accent-dark); text-decoration:underline;">Datenschutzerklärung</a>.
  </p>
  <div style="display:flex; gap:10px;">
    <button id="cookie-reject" type="button" style="flex:1; background:transparent; color:var(--ink3); border:1px solid #D8D3CC; border-radius:8px; padding:10px; font-size:13px; font-weight:600; cursor:pointer;">Ablehnen</button>
    <button id="cookie-accept" type="button" style="flex:1; background:var(--accent); color:#1A1A1A; border:none; border-radius:8px; padding:10px; font-size:13px; font-weight:600; cursor:pointer;">Akzeptieren</button>
  </div>
</div>
<script>
  (function () {
    var KEY = "gr_cookie_consent_v1";
    var banner = document.getElementById("cookie-banner");
    if (!banner) return;
    function hide() { banner.style.display = "none"; }
    function save(v) { try { localStorage.setItem(KEY, v); } catch (e) {} }
    var choice = null;
    try { choice = localStorage.getItem(KEY); } catch (e) {}
    if (!choice) {
      banner.style.display = "block";
    }
    document.getElementById("cookie-accept").addEventListener("click", function () {
      save("accepted");
      window.__marketingConsent = true;
      if (typeof gtag === "function") gtag('consent', 'update', { 'analytics_storage': 'granted' });
      hide();
    });
    document.getElementById("cookie-reject").addEventListener("click", function () {
      save("rejected");
      window.__marketingConsent = false;
      if (typeof gtag === "function") gtag('consent', 'update', { 'analytics_storage': 'denied' });
      hide();
    });
  })();
</script>
<!-- End Cookie Banner -->

<!-- Passa atribuição (fbclid, utm_*, _fbp, _fbc) para o checkout (cross-domain) -->
<script>
  (function () {
    function getCookie(name) {
      var m = document.cookie.match("(^|;)\\s*" + name + "\\s*=\\s*([^;]+)");
      return m ? m.pop() : "";
    }
    function buildCheckoutUrl(base) {
      try {
        var url = new URL(base, window.location.origin);
        var current = new URLSearchParams(window.location.search);
        // repassa todos os params da URL atual (fbclid, utm_*, gclid, ttclid, etc.)
        current.forEach(function (value, key) { if (value) url.searchParams.set(key, value); });
        // cookies do Meta Pixel (existem após o consentimento)
        var fbp = getCookie("_fbp");
        var fbc = getCookie("_fbc");
        if (fbp) url.searchParams.set("fbp", fbp);
        if (fbc) {
          url.searchParams.set("fbc", fbc);
        } else if (current.get("fbclid")) {
          // monta fbc a partir do fbclid se o cookie ainda não existir
          url.searchParams.set("fbc", "fb.1." + Date.now() + "." + current.get("fbclid"));
        }
        // repassa o consentimento definido na primeira página para o checkout
        url.searchParams.set("consent", window.__marketingConsent ? "granted" : "denied");
        return url.toString();
      } catch (e) { return base; }
    }
    function refreshLinks() {
      var links = document.querySelectorAll("a[href*='alfima.com']");
      for (var i = 0; i < links.length; i++) {
        if (!links[i].dataset.base) links[i].dataset.base = links[i].getAttribute("href");
        links[i].setAttribute("href", buildCheckoutUrl(links[i].dataset.base));
      }
    }
    refreshLinks();
    // recalcula no clique para capturar cookies setados após o consentimento
    document.addEventListener("click", function (e) {
      var a = e.target && e.target.closest ? e.target.closest("a[href*='alfima.com']") : null;
      if (a) a.setAttribute("href", buildCheckoutUrl(a.dataset.base || a.getAttribute("href")));
    }, true);
  })();
</script>
<!-- End attribution passthrough -->

<script>
  (function () {
    var links = document.querySelectorAll("[data-setlang]");
    for (var i = 0; i < links.length; i++) {
      links[i].addEventListener("click", function (e) {
        e.preventDefault();
        try { localStorage.setItem("gr_lang", this.dataset.setlang); } catch (err) {}
        location.href = this.getAttribute("href") + location.search;
      });
    }
  })();
</script>

<script>
(function () {
  var SEEN_KEY = "gr_boost_pop_v1";
  var overlay = document.getElementById("bp-overlay");
  if (!overlay) return;
  function seen() { try { return !!localStorage.getItem(SEEN_KEY); } catch (e) { return true; } }
  function mark() { try { localStorage.setItem(SEEN_KEY, "1"); } catch (e) {} }
  function isLead() { try { return !!localStorage.getItem("gr_free_class_v1"); } catch (e) { return false; } }
  function close() { overlay.classList.remove("open"); }

  setTimeout(function () {
    if (seen() || isLead()) return;
    var fc = document.getElementById("fc-overlay");
    if (fc && fc.classList.contains("open")) return; // funil já aberto
    overlay.classList.add("open");
    mark(); // uma vez por visitante
    window.dataLayer = window.dataLayer || [];
    window.dataLayer.push({ event: "boost_popup_shown" });
  }, 10000);

  overlay.addEventListener("click", function (e) { if (e.target === overlay) close(); });
  document.getElementById("bp-close").addEventListener("click", close);
  document.getElementById("bp-later").addEventListener("click", close);
  document.getElementById("bp-cta").addEventListener("click", function () {
    close();
    window.dataLayer = window.dataLayer || [];
    window.dataLayer.push({ event: "boost_popup_cta" });
  });
  document.addEventListener("keydown", function (e) {
    if (e.key === "Escape" && overlay.classList.contains("open")) close();
  });
})();
</script>
<script>
  // Hero video: click-to-play (preload="none" keeps the file off the initial page load)
  document.querySelectorAll('[data-hero-video]').forEach(function (box) {
    var video = box.querySelector('video');
    var btn = box.querySelector('.hv-play');
    if (!video || !btn) return;
    btn.addEventListener('click', function () {
      box.classList.add('is-playing');
      video.play();
    });
    video.addEventListener('pause', function () {
      if (video.currentTime === 0) box.classList.remove('is-playing');
    });
  });
</script>
</body>
</html>
