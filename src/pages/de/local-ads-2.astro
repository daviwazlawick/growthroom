---
const CHECKOUT_URL = "https://alfima.com/daviwazl/ads-blueprint/purchase";
// TODO: publish when real testimonials are collected. Never invent testimonials, names, or photos.
const SHOW_TESTIMONIALS = false;
---

<!DOCTYPE html>
<html lang="de">
<head>
<!-- Google Tag Manager -->
<script is:inline>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
})(window,document,'script','dataLayer','GTM-KL573474');</script>
<!-- End Google Tag Manager -->
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Meta Ads für lokale Unternehmen — Bauen Sie Ihr Werbe-Fundament selbst | Launch Ads Blueprint</title>
<meta name="description" content="Bevor Sie Facebook- und Instagram-Anzeigen für Ihr lokales Unternehmen schalten: Bauen Sie Ihr Meta-Werbe-Fundament auf — Business Manager, Werbekonto, Pixel, Events und Ihre erste Kampagne. Schritt für Schritt, für komplette Einsteiger. Einmalig 27 €.">
<link rel="canonical" href="https://growthroom.eu/de/local-ads-2">
<link rel="alternate" hreflang="en" href="https://growthroom.eu/local-ads-2">
<link rel="alternate" hreflang="de" href="https://growthroom.eu/de/local-ads-2">
<link rel="alternate" hreflang="x-default" href="https://growthroom.eu/local-ads-2">
<script is:inline>
(function(){try{
  if (localStorage.getItem("gr_lang") === "en") location.replace(location.pathname.replace(/^\/de/, "") + location.search);
}catch(e){}})();
</script>

<!-- Meta Pixel: PageView dispara sempre no load; demais eventos exigem consentimento -->
<script>
  (function () {
    !function(f,b,e,v,n,t,s){if(f.fbq)return;n=f.fbq=function(){n.callMethod?
    n.callMethod.apply(n,arguments):n.queue.push(arguments)};if(!f._fbq)f._fbq=n;
    n.push=n;n.loaded=!0;n.version='2.0';n.queue=[];t=b.createElement(e);t.async=!0;
    t.src=v;s=b.getElementsByTagName(e)[0];s.parentNode.insertBefore(t,s)}(window,
    document,'script','https://connect.facebook.net/en_US/fbevents.js');
    fbq('init', '1009429968103557');
    fbq('track', 'PageView');
  })();

  var CONSENT_KEY = "gr_cookie_consent_v1";
  try { window.__marketingConsent = localStorage.getItem(CONSENT_KEY) === "accepted"; }
  catch (e) { window.__marketingConsent = false; }
  window.trackMarketing = function (event, params, options) {
    if (!window.__marketingConsent || typeof window.fbq !== "function") return;
    window.fbq("track", event, params || {}, options || {});
  };
</script>
<!-- End Meta Pixel -->

<!-- Google tag (gtag.js): Analytics gated by consent via Consent Mode -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-TRFL8MQJ5R"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('consent', 'default', { 'analytics_storage': 'denied' });
  if (window.__marketingConsent) {
    gtag('consent', 'update', { 'analytics_storage': 'granted' });
  }
  gtag('config', 'G-TRFL8MQJ5R');
</script>
<!-- End Google tag -->

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --bg:      #FFFFFF;
    --bg2:     #FAFAF9;
    --ink:     #18181B;
    --body:    #52525B;
    --muted:   #A1A1AA;
    --line:    #E4E4E7;
    --line2:   #F4F4F5;
    --accent:  #F59E0B;
    --accent-ink: #B45309;
    --accent-soft: #FFFBEB;
    --green:   #15803D;
  }

  html, body { overflow-x: hidden; max-width: 100%; }
  html {
    font-family: 'Inter', system-ui, -apple-system, sans-serif;
    font-size: 16px;
    -webkit-font-smoothing: antialiased;
    background: var(--bg);
    color: var(--ink);
    scroll-behavior: smooth;
  }
  a { color: inherit; text-decoration: none; }
  img { max-width: 100%; display: block; }

  /* ── Layout ── */
  .container { max-width: 1064px; margin: 0 auto; padding-left: 24px; padding-right: 24px; }
  .narrow { max-width: 640px; }
  .sec { padding-top: 104px; padding-bottom: 104px; }
  .sec-line { border-top: 1px solid var(--line); }
  .bg2 { background: var(--bg2); }
  @media (max-width: 640px) { .sec { padding-top: 72px; padding-bottom: 72px; } }

  /* ── Type ── */
  .kicker { display: inline-flex; align-items: center; gap: 8px; font-size: 13px; font-weight: 600; letter-spacing: .02em; color: var(--accent-ink); background: var(--accent-soft); border: 1px solid #FDE68A; border-radius: 999px; padding: 6px 14px; margin-bottom: 24px; }
  .h1 { font-size: clamp(38px, 5.4vw, 62px); font-weight: 700; letter-spacing: -.035em; line-height: 1.05; margin-bottom: 24px; }
  .h2 { font-size: clamp(28px, 3.6vw, 40px); font-weight: 700; letter-spacing: -.028em; line-height: 1.12; margin-bottom: 18px; }
  .h3 { font-size: 17px; font-weight: 600; letter-spacing: -.01em; margin-bottom: 8px; }
  .sub { font-size: 19px; line-height: 1.65; color: var(--body); }
  .p { font-size: 16px; line-height: 1.7; color: var(--body); }
  .small { font-size: 14px; line-height: 1.6; color: var(--body); }
  .tiny { font-size: 12.5px; line-height: 1.55; color: var(--muted); }
  .sec-intro { max-width: 620px; margin-bottom: 56px; }
  .sec-intro .p { font-size: 17px; }

  /* ── Buttons ── */
  .btn-dark {
    display: inline-flex; align-items: center; justify-content: center; gap: 8px;
    background: var(--ink); color: #fff;
    font-size: 15px; font-weight: 600; letter-spacing: -.005em;
    padding: 15px 28px; border-radius: 10px; border: none; cursor: pointer;
    transition: transform .12s, box-shadow .15s, background .15s;
    box-shadow: 0 1px 2px rgba(0,0,0,.12);
    font-family: inherit;
  }
  .btn-dark:hover { background: #000; transform: translateY(-1px); box-shadow: 0 6px 16px -6px rgba(0,0,0,.35); }
  .btn-ghost {
    display: inline-flex; align-items: center; justify-content: center; gap: 8px;
    background: var(--bg); color: var(--ink);
    font-size: 15px; font-weight: 600;
    padding: 15px 28px; border-radius: 10px; border: 1px solid var(--line); cursor: pointer;
    transition: border-color .15s, background .15s, transform .12s;
    font-family: inherit;
  }
  .btn-ghost:hover { border-color: var(--ink); transform: translateY(-1px); }
  .cta-row { display: flex; gap: 12px; flex-wrap: wrap; align-items: center; }
  @media (max-width: 640px) {
    .cta-row { flex-direction: column; align-items: stretch; }
    .btn-dark, .btn-ghost { width: 100%; }
  }

  /* ── Who this is for ── */
  .who { padding-top: 56px; padding-bottom: 64px; }
  .who-badges { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 20px; }
  .who-badge { font-size: 13.5px; font-weight: 500; color: var(--body); background: var(--bg); border: 1px solid var(--line); border-radius: 999px; padding: 8px 16px; }
  @media (max-width: 640px) { .who { padding-top: 44px; padding-bottom: 52px; } }

  /* ── Header ── */
  .topbar { border-bottom: 1px solid var(--line); background: rgba(255,255,255,.85); backdrop-filter: blur(8px); }
  .topbar-in { display: flex; align-items: center; justify-content: space-between; height: 64px; }
  .brand { font-size: 15px; font-weight: 700; letter-spacing: -.02em; display: flex; align-items: center; gap: 8px; }
  .brand-dot { width: 10px; height: 10px; border-radius: 3px; background: var(--accent); display: inline-block; }
  .topbar .btn-dark { padding: 9px 18px; font-size: 13.5px; border-radius: 8px; }
  .lang-switch { display: flex; gap: 2px; align-items: center; }
  .lang-switch a, .lang-switch span { font-size: 17px; line-height: 1; padding: 5px 7px; border-radius: 8px; border: 1px solid transparent; opacity: .45; cursor: pointer; }
  .lang-switch .active { opacity: 1; border-color: var(--line); background: var(--bg2); cursor: default; }
  .lang-switch a:hover { opacity: .8; }

  /* ── Hero ── */
  .hero { padding-top: 96px; padding-bottom: 88px; }
  @media (max-width: 640px) { .hero { padding-top: 64px; padding-bottom: 56px; } }
  .hero-sub { max-width: 600px; margin-bottom: 36px; }
  .hero-trust { display: flex; gap: 20px; flex-wrap: wrap; margin-top: 20px; }
  .hero-trust span { font-size: 13px; color: var(--muted); display: inline-flex; align-items: center; gap: 6px; }
  .hero-shot { margin-top: 64px; border: 1px solid var(--line); border-radius: 16px; overflow: hidden; box-shadow: 0 24px 60px -30px rgba(24,24,27,.25); }
  .hero-shot img { width: 100%; }
  @media (max-width: 640px) {
    .hero-trust { display: grid; grid-template-columns: 1fr 1fr; gap: 10px 16px; }
    .hero-shot { margin-top: 40px; }
    .sec-intro { margin-bottom: 40px; }
  }

  /* ── Cards ── */
  .grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; }
  .grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
  @media (max-width: 820px) { .grid-3 { grid-template-columns: 1fr; } .grid-2 { grid-template-columns: 1fr; } }
  .card { background: var(--bg); border: 1px solid var(--line); border-radius: 14px; padding: 28px; }
  .card .icon { width: 40px; height: 40px; border-radius: 10px; background: var(--accent-soft); border: 1px solid #FDE68A; display: flex; align-items: center; justify-content: center; margin-bottom: 18px; }
  .card .icon svg { width: 20px; height: 20px; stroke: var(--accent-ink); }

  /* ── Callout ── */
  .callout { background: var(--ink); color: #fff; border-radius: 16px; padding: 40px 44px; }
  .callout p { font-size: clamp(19px, 2.4vw, 24px); font-weight: 600; letter-spacing: -.015em; line-height: 1.4; }
  .callout span { color: var(--accent); }
  @media (max-width: 640px) { .callout { padding: 28px 24px; } }

  /* ── Is / Is not ── */
  .isnot .card ul { list-style: none; }
  .isnot .card li { display: flex; gap: 12px; align-items: flex-start; padding: 10px 0; font-size: 15px; line-height: 1.55; color: var(--body); border-top: 1px solid var(--line2); }
  .isnot .card li:first-of-type { border-top: none; }
  .isnot .card li svg { width: 18px; height: 18px; flex-shrink: 0; margin-top: 2px; }
  .li-yes svg { stroke: var(--green); }
  .li-no svg { stroke: var(--muted); }
  .isnot .label { font-size: 12px; font-weight: 600; letter-spacing: .08em; text-transform: uppercase; margin-bottom: 14px; display: block; }

  /* ── Build timeline ── */
  .build { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 14px; counter-reset: step; }
  @media (max-width: 900px) { .build { grid-template-columns: 1fr 1fr; } }
  @media (max-width: 620px) { .build { grid-template-columns: 1fr; } }
  .build-item { border: 1px solid var(--line); border-radius: 14px; padding: 22px; background: var(--bg); position: relative; }
  .build-num { font-size: 12px; font-weight: 600; color: var(--accent-ink); background: var(--accent-soft); border: 1px solid #FDE68A; border-radius: 999px; padding: 3px 10px; display: inline-block; margin-bottom: 14px; }
  .build-item .h3 { margin-bottom: 6px; }
  .build-item .small { color: var(--body); }

  /* ── Comparison table ── */
  .compare { border: 1px solid var(--line); border-radius: 16px; overflow: hidden; }
  .compare-head, .compare-row { display: grid; grid-template-columns: 1.1fr 1fr 1fr; }
  .compare-head { background: var(--bg2); border-bottom: 1px solid var(--line); }
  .compare-head div { padding: 16px 20px; font-size: 13px; font-weight: 600; letter-spacing: .02em; }
  .compare-head .col-own { color: var(--accent-ink); }
  .compare-row { border-top: 1px solid var(--line2); }
  .compare-row:first-of-type { border-top: none; }
  .compare-row div { padding: 16px 20px; font-size: 14px; line-height: 1.55; }
  .compare-row .rowlab { font-weight: 600; color: var(--ink); font-size: 13.5px; }
  .compare-row .blind { color: var(--muted); }
  .compare-row .own { color: var(--ink); background: #FFFDF6; }
  @media (max-width: 680px) {
    .compare-head { display: none; }
    .compare-row { grid-template-columns: 1fr; padding: 14px 0; }
    .compare-row div { padding: 4px 20px; }
    .compare-row .rowlab { padding-top: 8px; }
    .compare-row .blind::before { content: "Blind auslagern — "; font-weight: 600; color: var(--muted); }
    .compare-row .own::before { content: "Selbst besitzen — "; font-weight: 600; color: var(--accent-ink); }
    .compare-row .own { background: transparent; }
  }

  /* ── After checklist ── */
  .after-list { list-style: none; max-width: 620px; }
  .after-list li { display: flex; gap: 14px; align-items: flex-start; padding: 14px 0; border-top: 1px solid var(--line2); font-size: 16px; line-height: 1.6; color: var(--body); }
  .after-list li:first-child { border-top: none; }
  .after-list svg { width: 20px; height: 20px; stroke: var(--green); flex-shrink: 0; margin-top: 3px; }

  /* ── FAQ ── */
  .faq { max-width: 720px; }
  .faq details { border-top: 1px solid var(--line); }
  .faq details:last-child { border-bottom: 1px solid var(--line); }
  .faq summary { cursor: pointer; list-style: none; display: flex; justify-content: space-between; align-items: center; gap: 16px; padding: 22px 4px; font-size: 16.5px; font-weight: 600; letter-spacing: -.01em; }
  .faq summary::-webkit-details-marker { display: none; }
  .faq summary::after { content: "+"; font-size: 20px; font-weight: 400; color: var(--muted); transition: transform .15s; }
  .faq details[open] summary::after { transform: rotate(45deg); }
  .faq .p { padding: 0 4px 24px; max-width: 640px; }

  /* ── Pricing ── */
  .price-card { border: 1px solid var(--line); border-radius: 20px; padding: 44px; background: var(--bg); box-shadow: 0 24px 60px -40px rgba(24,24,27,.3); max-width: 520px; }
  .price-row { display: flex; align-items: baseline; gap: 12px; margin: 8px 0 4px; }
  .price-big { font-size: 56px; font-weight: 700; letter-spacing: -.03em; }
  .price-was { font-size: 20px; color: var(--muted); text-decoration: line-through; }
  .price-inc { list-style: none; margin: 24px 0 28px; }
  .price-inc li { display: flex; gap: 10px; align-items: flex-start; padding: 7px 0; font-size: 14.5px; color: var(--body); }
  .price-inc svg { width: 17px; height: 17px; stroke: var(--green); flex-shrink: 0; margin-top: 3px; }
  @media (max-width: 640px) { .price-card { padding: 28px 22px; } }

  /* ── Footer ── */
  footer { border-top: 1px solid var(--line); padding: 40px 0 56px; }
  footer .tiny a { text-decoration: underline; text-underline-offset: 3px; }

  /* ── Free lessons modal ── */
  #fc-overlay { position: fixed; inset: 0; z-index: 10000; background: rgba(24,24,27,.5); backdrop-filter: blur(3px); display: none; align-items: flex-start; justify-content: center; overflow-y: auto; padding: 24px; }
  #fc-overlay.open { display: flex; }
  .fc-modal { background: var(--bg); border: 1px solid var(--line); border-radius: 18px; max-width: 640px; width: 100%; margin: auto; position: relative; padding: 36px 32px; }
  @media (max-width: 480px) { .fc-modal { padding: 26px 20px; } #fc-overlay { padding: 12px; } }
  .fc-close { position: absolute; top: 14px; right: 14px; background: none; border: none; font-size: 15px; color: var(--muted); cursor: pointer; padding: 6px; }
  .fc-close:hover { color: var(--ink); }
  .fc-kicker { font-size: 12px; font-weight: 600; letter-spacing: .08em; text-transform: uppercase; color: var(--accent-ink); display: block; margin-bottom: 12px; }
  .fc-h { font-size: 26px; font-weight: 700; letter-spacing: -.025em; margin-bottom: 10px; }
  .fc-field { margin-bottom: 14px; }
  .fc-field label { display: block; font-size: 13px; font-weight: 600; margin-bottom: 6px; }
  .fc-field input { width: 100%; border: 1px solid var(--line); background: var(--bg2); padding: 13px 15px; font-size: 15px; font-family: inherit; color: var(--ink); border-radius: 10px; }
  .fc-field input:focus { outline: 2px solid var(--accent); outline-offset: -1px; }
  .fc-error { display: none; font-size: 13px; color: #B91C1C; margin-top: 10px; }
  .fc-video video { width: 100%; display: block; background: #000; aspect-ratio: 16 / 9; border: 1px solid var(--line); border-radius: 12px; }
  .fc-lessons { border: 1px solid var(--line); border-radius: 12px; overflow: hidden; margin-top: 20px; }
  .fc-lesson { display: flex; gap: 12px; align-items: center; padding: 11px 16px; border-top: 1px solid var(--line2); font-size: 13.5px; color: var(--body); }
  .fc-lesson:first-child { border-top: none; }
  .fc-lesson .num { font-size: 11.5px; font-weight: 600; color: var(--accent-ink); flex-shrink: 0; }
  .fc-lesson .fc-tag { margin-left: auto; font-size: 11.5px; flex-shrink: 0; }
  .fc-lesson.locked { color: var(--muted); background: var(--bg2); cursor: pointer; }
  .fc-lesson.locked:hover { background: var(--accent-soft); }
  .fc-lesson.fc-playable { cursor: pointer; transition: background .15s; }
  .fc-lesson.fc-playable:hover { background: var(--accent-soft); }
  .fc-lesson.fc-playable.active .fc-tag { color: var(--accent-ink); font-weight: 600; }
  .fc-lock-note { display: none; padding: 16px; background: var(--accent-soft); border-top: 1px solid var(--line2); }
  .fc-lock-note p { font-size: 13.5px; line-height: 1.55; color: var(--body); margin-bottom: 12px; }
  .fc-lock-note strong { color: var(--ink); }
  .fc-offer { background: var(--ink); color: #fff; border-radius: 14px; padding: 26px; margin-top: 20px; }
  .fc-offer .btn-light { display: block; width: 100%; background: #fff; color: var(--ink); padding: 15px; font-size: 14.5px; font-weight: 600; text-align: center; border: none; border-radius: 10px; cursor: pointer; }
  .fc-offer .btn-light:hover { background: var(--bg2); }
  .fc-attn { animation: fcPulse 1.2s ease-in-out 3; }
  @keyframes fcPulse {
    0%, 100% { box-shadow: 0 0 0 0 rgba(245,158,11,0); }
    50%      { box-shadow: 0 0 0 6px rgba(245,158,11,.45); }
  }

  /* ── Boost popup ── */
  #bp-overlay { position: fixed; inset: 0; z-index: 9990; background: rgba(24,24,27,.5); backdrop-filter: blur(3px); display: none; align-items: center; justify-content: center; padding: 24px; }
  #bp-overlay.open { display: flex; }
  .bp-modal { background: var(--bg); border: 1px solid var(--line); border-radius: 18px; max-width: 470px; width: 100%; padding: 34px 30px 28px; position: relative; box-shadow: 0 30px 80px -30px rgba(0,0,0,.45); animation: bpIn .3s ease; }
  @keyframes bpIn { from { opacity: 0; transform: translateY(12px); } to { opacity: 1; transform: none; } }
  .bp-close { position: absolute; top: 12px; right: 12px; background: none; border: none; font-size: 15px; color: var(--muted); cursor: pointer; padding: 6px; }
  .bp-close:hover { color: var(--ink); }
  .bp-kicker { display: inline-block; font-size: 12px; font-weight: 600; letter-spacing: .06em; text-transform: uppercase; color: var(--accent-ink); background: var(--accent-soft); border: 1px solid #FDE68A; border-radius: 999px; padding: 5px 12px; margin-bottom: 16px; }
  .bp-h { font-size: 24px; font-weight: 700; letter-spacing: -.025em; line-height: 1.15; margin-bottom: 12px; }
  .bp-p { font-size: 14.5px; line-height: 1.65; color: var(--body); margin-bottom: 20px; }
  .bp-p strong { color: var(--ink); }
  .bp-sub { font-size: 12px; color: var(--muted); text-align: center; margin-top: 10px; }
  .bp-later { display: block; margin: 14px auto 0; background: none; border: none; font-family: inherit; font-size: 12.5px; color: var(--muted); cursor: pointer; text-decoration: underline; text-underline-offset: 3px; }
  .bp-later:hover { color: var(--ink); }
</style>

<!-- FAQ structured data -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    { "@type": "Question", "name": "Ich bin nicht technisch. Schaffe ich das wirklich?", "acceptedAnswer": { "@type": "Answer", "text": "Ja. Wenn Sie eine Datei an eine E-Mail anhängen können, können Sie das. Jede Lektion zeigt den genauen Bildschirm und den genauen Klick. Pausieren, klicken, weiter." } },
    { "@type": "Question", "name": "Ich habe keine Zeit — ich führe ein Unternehmen.", "acceptedAnswer": { "@type": "Answer", "text": "Der ganze Kurs besteht aus rund zwei Stunden kurzer Lektionen. Die meisten Inhaber schaffen das Setup an einem Wochenende — und diese Arbeit machen Sie nur einmal." } },
    { "@type": "Question", "name": "Ich habe noch nie eine Anzeige geschaltet.", "acceptedAnswer": { "@type": "Answer", "text": "Perfekt. Der Kurs setzt null Erfahrung voraus. Er beginnt vor Schritt eins: Ihr Meta Business Portfolio, von einem leeren Bildschirm aus." } },
    { "@type": "Question", "name": "Ich hatte schon Agenturen. Warum sollte ich das brauchen?", "acceptedAnswer": { "@type": "Answer", "text": "Weil Ihnen danach Ihr Werbekonto, Ihr Pixel und Ihre Daten gehören — und Sie die Berichte lesen können, für die Sie bezahlen. Wer die Arbeit versteht, ist ein besserer Kunde." } },
    { "@type": "Question", "name": "Bringt mir das Kunden?", "acceptedAnswer": { "@type": "Answer", "text": "Dieser Kurs baut Ihr Werbe-Fundament — das Setup, das stimmen muss, bevor irgendeine Anzeige funktionieren kann. Ein korrektes Fundament garantiert keine Ergebnisse; das kann ehrlicherweise niemand versprechen. Es verhindert die teuren Fehler und gibt Ihnen die Kontrolle über alles Weitere." } },
    { "@type": "Question", "name": "Ist der Kurs auf Deutsch?", "acceptedAnswer": { "@type": "Answer", "text": "Der Kurs ist auf Englisch — in einfacher, klarer Sprache, und jeder Klick wird am Bildschirm gezeigt. Der Meta-Werbeanzeigenmanager funktioniert überall gleich; Sie können Ihre Meta-Oberfläche auf Deutsch nutzen, während Sie den Lektionen folgen." } },
    { "@type": "Question", "name": "Ist das ein Abo?", "acceptedAnswer": { "@type": "Answer", "text": "Nein. Einmal 27 €. Lebenslanger Zugriff auf alle 20 Lektionen und jedes künftige Update." } },
    { "@type": "Question", "name": "Wie funktioniert die Rückerstattung?", "acceptedAnswer": { "@type": "Answer", "text": "Schreiben Sie innerhalb von 30 Tagen nach dem Kauf eine E-Mail — Sie erhalten jeden Cent zurück. Keine Formulare, keine Fragen." } }
  ]
}
</script>
</head>
<body>
<!-- Google Tag Manager (noscript) -->
<noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-KL573474"
height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
<!-- End Google Tag Manager (noscript) -->

<!-- HEADER -->
<div class="topbar">
  <div class="container topbar-in">
    <span class="brand"><span class="brand-dot"></span> Growth Room</span>
    <div style="display:flex; align-items:center; gap:14px;">
      <div class="lang-switch"><a href="/local-ads-2" data-setlang="en" title="English">🇬🇧</a><span class="active" title="Deutsch">🇩🇪</span></div>
      <a href={CHECKOUT_URL} target="_blank" rel="noopener" class="btn-dark">Setup starten — 27 €</a>
    </div>
  </div>
</div>

<!-- 1 · HERO -->
<section class="hero">
  <div class="container">
    <span class="kicker">Für lokale Unternehmer · Keine Vorkenntnisse nötig</span>
    <h1 class="h1">Hören Sie auf, mit „Boosten“ Geld <br>an Facebook &amp; Instagram zu spenden.</h1>
    <p class="sub hero-sub" style="margin-bottom:16px;">Launch Ads Blueprint führt Sie durch den Aufbau des Werbe-Fundaments hinter jeder Facebook- und Instagram-Kampagne — von null bis zu Ihrer ersten geschalteten Anzeige. Schritt für Schritt, Klick für Klick.</p>
    <p class="sub hero-sub" style="font-weight:500; color:var(--ink);">Ob Sie Ihre Anzeigen selbst schalten oder jemanden beauftragen: Sie wissen genau, was mit Ihrem Geld passiert.</p>
    <div class="cta-row">
      <a href={CHECKOUT_URL} target="_blank" rel="noopener" class="btn-dark">Setup starten — 27 €</a>
      <button type="button" class="btn-ghost" data-free-class>Die ersten 2 Lektionen kostenlos ansehen →</button>
    </div>
    <div class="hero-trust">
      <span>✓ Einmalzahlung</span>
      <span>✓ 20 kurze Lektionen</span>
      <span>✓ 30-Tage-Geld-zurück-Garantie</span>
      <span>✓ Alles, was Sie aufbauen, gehört Ihnen</span>
    </div>
    <div class="hero-shot">
      <img src="/img/launch-ads-mockup.png" alt="Launch Ads Blueprint — Meta-Ads-Kurs für lokale Unternehmen, Schritt für Schritt" />
    </div>
  </div>
</section>

<!-- WHO THIS IS FOR -->
<section class="sec-line who">
  <div class="container">
    <h2 class="h2" style="font-size:clamp(24px,3vw,32px); margin-bottom:12px;">Gemacht für Unternehmer, nicht für Marketer.</h2>
    <div class="who-badges">
      <span class="who-badge">Zahnärzte</span>
      <span class="who-badge">Ärzte</span>
      <span class="who-badge">Praxen &amp; Kliniken</span>
      <span class="who-badge">Physiotherapeuten</span>
      <span class="who-badge">Chiropraktiker</span>
      <span class="who-badge">Anwälte</span>
      <span class="who-badge">Restaurants</span>
      <span class="who-badge">Cafés</span>
      <span class="who-badge">Fitnessstudios</span>
      <span class="who-badge">Beauty-Salons</span>
      <span class="who-badge">Barbershops</span>
      <span class="who-badge">Immobilien</span>
      <span class="who-badge">Haus- &amp; Gartenservice</span>
      <span class="who-badge">Handwerksbetriebe</span>
      <span class="who-badge">Coaches</span>
      <span class="who-badge">Berater</span>
      <span class="who-badge">Lokale Dienstleister</span>
    </div>
    <p class="p" style="margin-top:20px; font-weight:500; color:var(--ink);">Wenn Ihr Unternehmen von einem stetigen Strom neuer Kunden lebt, wurde dieser Kurs für Sie gebaut.</p>
  </div>
</section>

<!-- 2 · PROBLEM -->
<section class="sec sec-line bg2">
  <div class="container">
    <div class="sec-intro">
      <h2 class="h2">Die meisten lokalen Unternehmen verlieren kein Geld mit Anzeigen.<br>Sie verlieren es, bevor die Anzeigen überhaupt laufen.</h2>
      <p class="p">Zahnarztpraxis, Restaurant, Fitnessstudio, Salon, Kanzlei — das Muster ist immer gleich. Das Geld verschwindet an drei Stellen, und keine davon ist die Anzeige selbst.</p>
    </div>
    <div class="grid-3">
      <div class="card">
        <div class="icon"><svg viewBox="0 0 24 24" fill="none" stroke-width="1.8"><path d="M7 17l5-10 5 10M5 21h14"/></svg></div>
        <h3 class="h3">Der Boost-Button</h3>
        <p class="small">50 € für „Beitrag bewerben“. 200 Likes. Null Kunden. Boosten fühlt sich wie Werbung an — aber ohne echte Kampagne dahinter ist es eine Spende an Meta.</p>
      </div>
      <div class="card">
        <div class="icon"><svg viewBox="0 0 24 24" fill="none" stroke-width="1.8"><path d="M12 8v4l3 3M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg></div>
        <h3 class="h3">Die blinde Agentur-Pauschale</h3>
        <p class="small">400–1.000 € oder mehr an eine Agentur, deren Arbeit Sie nicht beurteilen können. Gute Agenturen gibt es. Aber Sie erkennen sie nicht, wenn Sie die Arbeit nicht verstehen.</p>
      </div>
      <div class="card">
        <div class="icon"><svg viewBox="0 0 24 24" fill="none" stroke-width="1.8"><path d="M3 3v18h18M9 17V9m4 8v-5m4 5V7"/></svg></div>
        <h3 class="h3">Das unsichtbare Tracking</h3>
        <p class="small">Anzeigen laufen ohne Pixel und ohne Events. Meta kann nicht optimieren, was es nicht messen kann. Die meisten lokalen Werbekonten laufen blind — und der Inhaber merkt es nie.</p>
      </div>
    </div>
  </div>
</section>

<!-- 3 · WHY THIS HAPPENS -->
<section class="sec sec-line">
  <div class="container">
    <div class="narrow">
      <h2 class="h2">Sie sind nicht schlecht darin.<br>Ihnen hat nur niemand das Fundament gezeigt.</h2>
      <p class="p" style="margin-bottom:18px;">YouTube lehrt Hacks und „Gewinner-Strategien“ — für Leute, deren Konten längst eingerichtet sind. Agenturen machen das Setup für Sie und nehmen das Verständnis mit, wenn der Vertrag endet. Und der Werbeanzeigenmanager selbst wurde für Profis gebaut — nicht für jemanden, der zwischen zwei Patienten eine Praxis führt.</p>
      <p class="p" style="margin-bottom:18px;">So stehen kluge Unternehmer — Menschen, die Restaurants, Praxen und Kanzleien führen — verloren vor einem Dashboard. Das ist kein Kompetenzproblem. Es ist ein Reihenfolge-Problem.</p>
      <p class="p" style="margin-bottom:36px;">Facebook-Werbung hat die Schritte 1 bis 9. Fast alle beginnen bei Schritt 5.</p>
      <div class="callout">
        <p>Lagern Sie nichts aus, was Sie nicht verstehen. <span>Bauen Sie zuerst Ihr Fundament. Entscheiden Sie dann, wer Ihre Anzeigen betreut.</span></p>
      </div>
      <div class="cta-row" style="margin-top:28px;">
        <button type="button" class="btn-ghost" data-free-class>Die ersten 2 Lektionen kostenlos ansehen →</button>
      </div>
    </div>
  </div>
</section>

<!-- 4 · INTRODUCE -->
<section class="sec sec-line bg2">
  <div class="container">
    <div class="sec-intro">
      <span class="kicker">Launch Ads Blueprint</span>
      <h2 class="h2">Ein geführtes Setup.<br>Kein weiterer Marketing-Kurs.</h2>
      <p class="p">20 kurze Videolektionen. Sie schauen, Sie klicken, Sie bauen. Am Ende existiert Ihr Werbe-Fundament — in Ihren Konten, unter Ihren Logins, in Ihrem Besitz. Die meisten Inhaber schaffen das Setup an einem Wochenende.</p>
    </div>
    <div class="grid-2 isnot">
      <div class="card">
        <span class="label" style="color:var(--green);">Was das ist</span>
        <ul>
          <li class="li-yes"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>Eine Schritt-für-Schritt-Anleitung durch das komplette Meta-Setup — vom leeren Bildschirm bis zur ersten laufenden Kampagne</li>
          <li class="li-yes"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>Gemacht für komplette Einsteiger — jeder Klick wird am Bildschirm gezeigt</li>
          <li class="li-yes"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>Echte Assets, die Ihnen gehören: Business Manager, Werbekonto, Pixel, Events</li>
          <li class="li-yes"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>Auf Englisch — in einfacher, klarer Sprache. Kein Fachbegriff ohne Erklärung</li>
          <li class="li-yes"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>Eine Zahlung. Lebenslanger Zugriff, inklusive Updates</li>
        </ul>
      </div>
      <div class="card">
        <span class="label" style="color:var(--muted);">Was das nicht ist</span>
        <ul>
          <li class="li-no"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M6 6l12 12M18 6L6 18"/></svg>Ein Kurs für fortgeschrittenes Media Buying</li>
          <li class="li-no"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M6 6l12 12M18 6L6 18"/></svg>Ein Kurs über Skalierung oder die „7-stellige Agentur“</li>
          <li class="li-no"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M6 6l12 12M18 6L6 18"/></svg>Ein Strategie-Kurs für Leadgenerierung</li>
          <li class="li-no"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M6 6l12 12M18 6L6 18"/></svg>Ein Versprechen garantierter Leads, Verkäufe oder ROI</li>
          <li class="li-no"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M6 6l12 12M18 6L6 18"/></svg>Ein Abo</li>
        </ul>
      </div>
    </div>
    <div class="cta-row" style="margin-top:36px;">
      <a href={CHECKOUT_URL} target="_blank" rel="noopener" class="btn-dark">Setup starten — 27 €</a>
      <button type="button" class="btn-ghost" data-free-class>Die ersten 2 Lektionen kostenlos ansehen →</button>
    </div>
  </div>
</section>

<!-- 5 · EVERYTHING YOU'LL BUILD -->
<section class="sec sec-line">
  <div class="container">
    <div class="sec-intro">
      <h2 class="h2">Alles, was Sie aufbauen —<br>in genau der richtigen Reihenfolge.</h2>
      <p class="p">Das ist die Reihenfolge, der Profis folgen — genau die, die Einsteigern niemand zeigt. Jedes Teil existiert in Ihrem eigenen Meta-Business-Konto, wenn die Lektion endet.</p>
    </div>
    <div class="build">
      <div class="build-item">
        <span class="build-num">Schritt 1</span>
        <h3 class="h3">Meta Business Portfolio</h3>
        <p class="small">Das offizielle Zuhause Ihres Unternehmens bei Meta. Korrekt angelegt, auf Ihren Namen — nicht auf den einer Agentur.</p>
      </div>
      <div class="build-item">
        <span class="build-num">Schritt 2</span>
        <h3 class="h3">Business Manager</h3>
        <p class="small">Ein Ort, dem Ihre Seiten, Werbekonten und Berechtigungen gehören. Wer worauf zugreifen darf, entscheiden Sie.</p>
      </div>
      <div class="build-item">
        <span class="build-num">Schritt 3</span>
        <h3 class="h3">Werbekonto</h3>
        <p class="small">Von Tag eins an sauber aufgesetzt. Wenn Sie je mit einer Agentur arbeiten, vergeben Sie Zugriff — Sie geben keine Schlüssel ab.</p>
      </div>
      <div class="build-item">
        <span class="build-num">Schritt 4</span>
        <h3 class="h3">Abrechnung</h3>
        <p class="small">Zahlung korrekt konfiguriert, damit Ausgaben Sie nie überraschen — und keine Kampagne mitten im Lauf stoppt.</p>
      </div>
      <div class="build-item">
        <span class="build-num">Schritt 5</span>
        <h3 class="h3">Meta Pixel</h3>
        <p class="small">Das kleine Stück Code, das Ihnen zeigt, was nach dem Klick wirklich funktioniert. Gemeinsam installiert und getestet.</p>
      </div>
      <div class="build-item">
        <span class="build-num">Schritt 6</span>
        <h3 class="h3">Events</h3>
        <p class="small">Definieren Sie, was für Ihr Unternehmen zählt — Anrufe, Termine, Anfragen — damit Meta auf Kunden optimiert, nicht auf Klicks.</p>
      </div>
      <div class="build-item">
        <span class="build-num">Schritt 7</span>
        <h3 class="h3">Kampagnen</h3>
        <p class="small">Was Kampagnenziele wirklich bedeuten, in einfachen Worten — damit Sie bewusst das richtige wählen und selbstsicher starten.</p>
      </div>
      <div class="build-item">
        <span class="build-num">Schritt 8</span>
        <h3 class="h3">Anzeigengruppen</h3>
        <p class="small">Budgets, Zielgruppen und Platzierungen — die Ebene, auf der Einsteiger-Budgets leise versickern.</p>
      </div>
      <div class="build-item">
        <span class="build-num">Schritt 9</span>
        <h3 class="h3">Ihre erste Anzeige, live</h3>
        <p class="small">Anzeige bauen, Checkliste durchgehen, einschalten — korrekt, mit funktionierendem Tracking.</p>
      </div>
    </div>
    <p class="tiny" style="margin-top:24px;">All das entsteht in Ihren eigenen Konten, mit Ihren eigenen Logins. Nichts liegt bei uns, einer Agentur oder sonst jemandem. Jeder Kunde, den Ihre Anzeigen je bringen, läuft durch dieses System.</p>
    <div class="cta-row" style="margin-top:28px;">
      <button type="button" class="btn-ghost" data-free-class>Die ersten 2 Lektionen kostenlos — richten Sie Ihr Konto mit mir ein →</button>
    </div>
  </div>
</section>

<!-- THREE MONTHS FROM NOW -->
<section class="sec sec-line">
  <div class="container">
    <div class="narrow">
      <span class="kicker">Nach dem Setup</span>
      <h2 class="h2">In drei Monaten.</h2>
      <p class="sub" style="margin-bottom:16px;">Sie öffnen den Werbeanzeigenmanager — und nichts auf dem Bildschirm macht Ihnen mehr Angst.</p>
      <p class="p" style="margin-bottom:16px;">Sie wissen, was läuft, was es kostet und was es zurückbringt. Wenn eine Agentur ein Angebot schickt, lesen Sie es wie jemand, der die Arbeit selbst gemacht hat — weil Sie genau das getan haben.</p>
      <p class="p" style="margin-bottom:16px;">Nichts an Ihrem Marketing ist ein Geheimnis, für dessen Bewahrung Sie jemand anderen bezahlen.</p>
      <p class="p" style="font-weight:500; color:var(--ink);">Das ist kein Versprechen von Kunden über Nacht. Es ist etwas Leiseres — und Wertvolleres: sich beim eigenen Geld nie wieder verloren zu fühlen.</p>
      <div class="cta-row" style="margin-top:28px;">
        <button type="button" class="btn-ghost" data-free-class>Die ersten 2 Lektionen kostenlos ansehen →</button>
      </div>
    </div>
  </div>
</section>

<!-- 6 · WHY THIS MATTERS -->
<section class="sec sec-line bg2">
  <div class="container">
    <div class="sec-intro">
      <h2 class="h2">Das Setup ist technisch.<br>Der Gewinn ist es nicht.</h2>
      <p class="p">Jedes Teil, das Sie aufbauen, wird zu etwas, das Sie in Ihrem Unternehmen jeden Monat spüren.</p>
    </div>
    <div class="grid-3">
      <div class="card">
        <div class="icon"><svg viewBox="0 0 24 24" fill="none" stroke-width="1.8"><path d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4"/></svg></div>
        <h3 class="h3">Kontrolle</h3>
        <p class="small">Jedes Konto auf Ihren Namen. Agentur wechseln, Anzeigen pausieren, Zahlen prüfen — nichts wird Ihnen je vorenthalten.</p>
      </div>
      <div class="card">
        <div class="icon"><svg viewBox="0 0 24 24" fill="none" stroke-width="1.8"><path d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/><path d="M2.5 12C4.3 7.3 7.9 4.5 12 4.5s7.7 2.8 9.5 7.5c-1.8 4.7-5.4 7.5-9.5 7.5S4.3 16.7 2.5 12z"/></svg></div>
        <h3 class="h3">Klarheit</h3>
        <p class="small">Sie lesen Ihre eigenen Berichte und wissen, wofür Sie bezahlen. „Wie laufen die Anzeigen?“ ist keine Frage mehr, vor der Sie sich fürchten.</p>
      </div>
      <div class="card">
        <div class="icon"><svg viewBox="0 0 24 24" fill="none" stroke-width="1.8"><path d="M13 10V3L4 14h7v7l9-11h-7z"/></svg></div>
        <h3 class="h3">Unabhängigkeit</h3>
        <p class="small">Selbst schalten oder Hilfe holen — aus einer Position des Verstehens, nicht des Hoffens. Sie entscheiden, und Sie können Ihre Meinung ändern.</p>
      </div>
    </div>
  </div>
</section>

<!-- 7 · AGENCY VS UNDERSTANDING -->
<section class="sec sec-line">
  <div class="container">
    <div class="sec-intro">
      <h2 class="h2">Das ist nicht gegen Agenturen.<br>Es ist gegen Blindflug.</h2>
      <p class="p">Experten zu beauftragen kann eine großartige Entscheidung sein — wenn Sie verstehen, was Sie kaufen. Das hier ändert sich, wenn Ihr Fundament zuerst Ihnen selbst gehört.</p>
    </div>
    <div class="compare">
      <div class="compare-head">
        <div></div>
        <div>Blind auslagern</div>
        <div class="col-own">Ihr Fundament besitzen</div>
      </div>
      <div class="compare-row">
        <div class="rowlab">Wem das Werbekonto gehört</div>
        <div class="blind">Oft der Agentur</div>
        <div class="own">Ihnen. Immer.</div>
      </div>
      <div class="compare-row">
        <div class="rowlab">Wenn der Vertrag endet</div>
        <div class="blind">Konten und Daten können mitgehen</div>
        <div class="own">Alles bleibt bei Ihrem Unternehmen</div>
      </div>
      <div class="compare-row">
        <div class="rowlab">Die Arbeit beurteilen</div>
        <div class="blind">Sie vertrauen Berichten, die Sie nicht lesen können</div>
        <div class="own">Sie lesen die Zahlen selbst</div>
      </div>
      <div class="compare-row">
        <div class="rowlab">Kosten für den Start</div>
        <div class="blind">400–1.000 € Setup, danach Pauschale</div>
        <div class="own">27 €, einmal</div>
      </div>
      <div class="compare-row">
        <div class="rowlab">Wenn etwas kaputtgeht</div>
        <div class="blind">Sie warten auf eine Antwort</div>
        <div class="own">Sie wissen, wo Sie nachsehen</div>
      </div>
      <div class="compare-row">
        <div class="rowlab">Später Experten beauftragen</div>
        <div class="blind">Ein Blindflug</div>
        <div class="own">Eine informierte Entscheidung</div>
      </div>
    </div>
    <p class="tiny" style="margin-top:20px;">Gute Agenturen werden sich freuen, dass Sie diesen Kurs gemacht haben. Die besten Kunden verstehen die Arbeit.</p>
  </div>
</section>

<!-- 8 · AFTER THE COURSE -->
<section class="sec sec-line bg2">
  <div class="container">
    <div class="sec-intro">
      <h2 class="h2">Wo Sie stehen —<br>nach der letzten Lektion.</h2>
    </div>
    <ul class="after-list">
      <li><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>Ihr Meta Business Portfolio und Business Manager existieren — aufgeräumt, auf Ihren Namen</li>
      <li><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>Werbekonto und Abrechnung sind konfiguriert und bereit, sicher Budget auszugeben</li>
      <li><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>Ihr Pixel ist installiert, getestet und feuert — mit Events, die zu Ihrem Unternehmen passen</li>
      <li><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>Sie verstehen Kampagnen, Anzeigengruppen und Anzeigen gut genug, um ein echtes Gespräch darüber zu führen</li>
      <li><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>Ihre erste Kampagne ist gebaut und live — korrekt, mit funktionierendem Tracking</li>
    </ul>
    <div class="grid-2" style="margin-top:48px; max-width:820px;">
      <div class="card">
        <h3 class="h3">Weg eins: selbst weitermachen</h3>
        <p class="small">Machen Sie allein weiter, mit Ihrem Budget, in Ihrem Tempo. Das Fundament, das Sie gebaut haben, ist genau das, worauf Ihre künftigen Kampagnen stehen.</p>
      </div>
      <div class="card">
        <h3 class="h3">Weg zwei: Hilfe holen — informiert</h3>
        <p class="small">Holen Sie Freelancer oder Agentur an Bord — im Wissen, was Sie kaufen, was geliefert werden muss und was Ihnen gehört. Sie unterschreiben nie wieder blind.</p>
      </div>
    </div>
    <div class="cta-row" style="margin-top:36px;">
      <a href={CHECKOUT_URL} target="_blank" rel="noopener" class="btn-dark">Setup starten — 27 €</a>
      <button type="button" class="btn-ghost" data-free-class>Die ersten 2 Lektionen kostenlos ansehen →</button>
    </div>
  </div>
</section>

<!-- 9 · TESTIMONIALS — oculto até haver depoimentos reais -->
<!-- TODO: publish when real testimonials are collected. Never invent testimonials, names, or photos. -->
{SHOW_TESTIMONIALS && (
<section class="sec sec-line">
  <div class="container">
    <div class="sec-intro">
      <h2 class="h2">Local owners who built their foundation.</h2>
    </div>
    <div class="grid-3">
      <div class="card">
        <h3 class="h3">[PLACEHOLDER — Student name]</h3>
        <p class="tiny" style="margin-bottom:10px;">[PLACEHOLDER — business type + city, e.g. "Physiotherapy clinic, Porto"]</p>
        <p class="small">[PLACEHOLDER — 2-sentence quote from a real local business owner. Never invent this.]</p>
      </div>
      <div class="card">
        <h3 class="h3">[PLACEHOLDER — Student name]</h3>
        <p class="tiny" style="margin-bottom:10px;">[PLACEHOLDER — business type + city]</p>
        <p class="small">[PLACEHOLDER — 2-sentence quote from a real local business owner. Never invent this.]</p>
      </div>
      <div class="card">
        <h3 class="h3">[PLACEHOLDER — Student name]</h3>
        <p class="tiny" style="margin-bottom:10px;">[PLACEHOLDER — business type + city]</p>
        <p class="small">[PLACEHOLDER — 2-sentence quote from a real local business owner. Never invent this.]</p>
      </div>
    </div>
  </div>
</section>
)}

<!-- 10 · FAQ -->
<section class="sec sec-line">
  <div class="container">
    <div class="sec-intro">
      <h2 class="h2">Fragen, die Unternehmer<br>vor dem Kauf stellen.</h2>
    </div>
    <div class="faq">
      <details>
        <summary>Ich bin nicht technisch. Schaffe ich das wirklich?</summary>
        <p class="p">Ja. Wenn Sie eine Datei an eine E-Mail anhängen können, können Sie das. Jede Lektion zeigt den genauen Bildschirm und den genauen Klick. Pausieren, klicken, weiter.</p>
      </details>
      <details>
        <summary>Ich habe keine Zeit — ich führe ein Unternehmen.</summary>
        <p class="p">Der ganze Kurs besteht aus rund zwei Stunden kurzer Lektionen. Ein Wochenende — und diese Arbeit machen Sie nur einmal. Das Fundament arbeitet weiter, solange Sie werben.</p>
      </details>
      <details>
        <summary>Ich habe noch nie eine Anzeige geschaltet.</summary>
        <p class="p">Perfekt. Der Kurs setzt null Erfahrung voraus. Er beginnt vor Schritt eins: Ihr Meta Business Portfolio, von einem leeren Bildschirm aus.</p>
      </details>
      <details>
        <summary>Ich hatte schon Agenturen. Warum sollte ich das brauchen?</summary>
        <p class="p">Weil Ihnen danach Ihr Werbekonto, Ihr Pixel und Ihre Daten gehören — und Sie die Berichte lesen können, für die Sie bezahlen. Wer die Arbeit versteht, ist ein besserer Kunde — und lässt sich schwerer über den Tisch ziehen.</p>
      </details>
      <details>
        <summary>Bringt mir das Kunden?</summary>
        <p class="p">Ehrliche Antwort: Dieser Kurs baut Ihr Werbe-Fundament — das Setup, das stimmen muss, bevor irgendeine Anzeige funktionieren kann. Ein korrektes Fundament garantiert keine Ergebnisse; das kann ehrlicherweise niemand versprechen. Es verhindert die teuren Fehler und gibt Ihnen die Kontrolle über alles Weitere.</p>
      </details>
      <details>
        <summary>Ist der Kurs auf Deutsch?</summary>
        <p class="p">Der Kurs ist auf Englisch — in einfacher, klarer Sprache, und jeder Klick wird am Bildschirm gezeigt. Der Meta-Werbeanzeigenmanager funktioniert überall gleich; Sie können Ihre Meta-Oberfläche auf Deutsch nutzen, während Sie den Lektionen folgen.</p>
      </details>
      <details>
        <summary>Ist das ein Abo?</summary>
        <p class="p">Nein. Einmal 27 €. Lebenslanger Zugriff auf alle 20 Lektionen und jedes künftige Update.</p>
      </details>
      <details>
        <summary>Wie funktioniert die Rückerstattung?</summary>
        <p class="p">Schreiben Sie mir innerhalb von 30 Tagen nach dem Kauf eine E-Mail. Sie bekommen jeden Cent zurück. Keine Formulare, kein „Nachweis, dass Sie es durchgearbeitet haben“, keine Fragen.</p>
      </details>
    </div>
  </div>
</section>

<!-- 11 · CTA -->
<section class="sec sec-line bg2">
  <div class="container" style="display:flex; flex-wrap:wrap; gap:48px; align-items:flex-start; justify-content:space-between;">
    <div style="max-width:440px;">
      <h2 class="h2">Bauen Sie Ihr Fundament.<br>Entscheiden Sie dann, wer Ihre Anzeigen betreut.</h2>
      <p class="p" style="margin-bottom:24px;">Ein Wochenende Setup. Ein Unternehmerleben lang wissen, wohin jeder Werbe-Euro fließt.</p>
      <p class="tiny">Einführungspreis für die ersten Teilnehmer. Er steigt auf 197 €, sobald die ersten Bewertungen da sind.</p>
    </div>
    <div class="price-card">
      <span class="kicker" style="margin-bottom:12px;">Launch Ads Blueprint</span>
      <div class="price-row">
        <span class="price-big">27 €</span>
        <span class="price-was">197 €</span>
      </div>
      <p class="small">Einmalzahlung. Kein Abo.</p>
      <ul class="price-inc">
        <li><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>Alle 20 Lektionen — vom leeren Bildschirm zur laufenden Kampagne</li>
        <li><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>Meta Ads Launch-Checkliste</li>
        <li><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>Kennzahlen-Guide für Einsteiger</li>
        <li><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>ChatGPT-Prompts für Anzeigentexte</li>
        <li><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>Lebenslanger Zugriff + alle Updates</li>
      </ul>
      <a href={CHECKOUT_URL} target="_blank" rel="noopener" class="btn-dark" style="display:flex; width:100%;">Setup starten — 27 €</a>
      <p class="tiny" style="text-align:center; margin-top:12px;">Sicherer Checkout über Alfima · Karte &amp; PayPal · Sofortiger Zugriff · 30-Tage-Geld-zurück-Garantie</p>
      <button type="button" class="btn-ghost" data-free-class style="width:100%; margin-top:10px;">Noch unsicher? Die ersten 2 Lektionen kostenlos ansehen →</button>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="container">
    <p class="tiny" style="margin-bottom:8px;">Launch Ads Blueprint ist ein unabhängiger Kurs von Growth Room. Er zeigt Ihnen, wie Sie Ihr Meta-Werbe-Fundament aufbauen; Ergebnisse aus Werbung hängen von Ihrem Angebot, Markt und Ihrer Umsetzung ab.</p>
    <p class="tiny"><a href="/privacy-policy">Datenschutz</a> · <a href="/terms">AGB</a></p>
  </div>
</footer>

<!-- FREE FIRST LESSONS MODAL -->
<div id="fc-overlay" role="dialog" aria-modal="true" aria-labelledby="fc-title">
  <div class="fc-modal">
    <button type="button" class="fc-close" id="fc-close" aria-label="Close">✕</button>

    <!-- State 1: capture -->
    <div id="fc-form-state">
      <span class="fc-kicker">Offene Vorschau</span>
      <h3 id="fc-title" class="fc-h">Zwei komplette Lektionen. Offen.</h3>
      <p class="small" style="margin-bottom:22px;">Lektion 1 zeigt, warum die meisten Einsteiger Geld mit Anzeigen verlieren. In Lektion 2 richten wir gemeinsam Ihr Meta-Business-Konto ein — Schritt für Schritt. Schauen Sie beide, machen Sie das Setup, entscheiden Sie dann. Sagen Sie mir nur, wer Sie sind. (Die Lektionen sind auf Englisch.)</p>
      <form id="fc-form" novalidate>
        <div class="fc-field">
          <label for="fc-name">Ihr Name</label>
          <input id="fc-name" name="fullName" type="text" autocomplete="name" placeholder="Jane Smith" required />
        </div>
        <div class="fc-field">
          <label for="fc-email">Ihre E-Mail</label>
          <input id="fc-email" name="email" type="email" autocomplete="email" placeholder="jane@example.com" required />
        </div>
        <button type="submit" class="btn-dark" id="fc-submit" style="display:flex; width:100%;">Lektionen 1 &amp; 2 öffnen →</button>
        <p class="fc-error" id="fc-error">Bitte geben Sie Ihren Namen und eine gültige E-Mail an.</p>
        <p class="tiny" style="margin-top:12px;">Ich schicke Ihnen den Lektions-Link auch per E-Mail, plus gelegentliche Ads-Tipps. Jederzeit abbestellbar. <a href="/privacy-policy" style="text-decoration:underline;">Datenschutzerklärung</a>.</p>
      </form>
    </div>

    <!-- State 2: video -->
    <div id="fc-video-state" style="display:none;">
      <span class="fc-kicker" id="fc-eyebrow">Lektionen 1 &amp; 2 von 20 — offene Vorschau</span>
      <h3 class="fc-h" id="fc-lesson-title">Why most beginners fail.</h3>
      <div class="fc-video">
        <video id="fc-player" src="/videos/lesson-1.mp4" controls controlsList="nodownload" preload="none" playsinline></video>
      </div>
      <a href={CHECKOUT_URL} target="_blank" rel="noopener" class="btn-dark" id="fc-buy-top" style="display:flex; width:100%; margin-top:16px;">Setup starten — 27 €</a>
      <p class="tiny" style="text-align:center; margin-top:8px;">Sicherer Checkout über Alfima · Karte &amp; PayPal · Sofortiger Zugriff · 30-Tage-Geld-zurück-Garantie</p>
      <button type="button" class="btn-ghost" id="fc-next-cta" style="width:100%; margin-top:10px;">▶ Nächste Lektion → Lektion 2 ansehen (weiter kostenlos)</button>
      <div class="fc-lessons">
        <div class="fc-lesson fc-playable" data-lesson="0"><span class="num">01</span> Why most beginners fail <span class="fc-tag">▶ Läuft gerade</span></div>
        <div class="fc-lesson fc-playable" data-lesson="1"><span class="num">02</span> Who the Meta Ads course is for <span class="fc-tag">▶ Kostenlos ansehen</span></div>
        <div class="fc-lesson locked"><span class="num">03</span> Meta Ads setup checklist <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">04</span> Creating the Facebook page <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">05</span> Adding Ads Manager and payment <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">06</span> Connecting Instagram to your Facebook ads account <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">07</span> Meta Events setup for ad tracking <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">08</span> How to install the Meta Pixel and test events <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">09</span> Meta Ads campaign goals explained <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">10</span> Setting up a Meta lead campaign ad set <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">11</span> Setting up a Meta lead campaign ad set — part 2 <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">12</span> Master Facebook Ads audiences and targeting <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">13</span> Meta Advantage+ targeting controls explained <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">14</span> Facebook Ads setup: excluding key countries <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">15</span> Building your first ad: image <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">16</span> Building your first ad: video <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">17</span> Final review before you hit publish <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">18</span> Wrapping up ads with some tips <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">19</span> Setting up events from Meta to optimise <span class="fc-tag">🔒</span></div>
        <div class="fc-lesson locked"><span class="num">20</span> Testing the event and final class <span class="fc-tag">🔒</span></div>
        <div class="fc-lock-note" id="fc-lock-note">
          <p id="fc-lock-text"></p>
          <a href={CHECKOUT_URL} target="_blank" rel="noopener" class="btn-dark" style="display:flex; width:100%;">Setup starten — 27 € →</a>
        </div>
      </div>
      <div class="fc-offer" id="fc-offer">
        <p class="tiny" style="color:#A1A1AA; letter-spacing:.08em; text-transform:uppercase; font-weight:600;">Bereit für Lektion 3?</p>
        <div class="price-row" style="margin:8px 0 4px;">
          <span class="price-big" style="font-size:40px; color:#fff;">€27</span>
          <span class="price-was" style="font-size:16px;">€197</span>
        </div>
        <p class="small" style="color:#D4D4D8; margin-bottom:8px;">Als Nächstes: Tracking. Die Pixel-Installation entscheidet, ob Meta Ihre Anzeigen überhaupt optimieren kann — und genau hier geben die meisten Einsteiger auf. Ich führe Sie Klick für Klick hindurch.</p>
        <p class="small" style="color:#D4D4D8; margin-bottom:18px;">Einmalzahlung · Schaltet alle 18 übrigen Lektionen + Boni frei — sofort auf der Alfima-Plattform.</p>
        <a href={CHECKOUT_URL} target="_blank" rel="noopener" class="btn-light">Nächste Lektion → Kaufen &amp; auf Alfima weiterschauen</a>
        <p class="tiny" style="text-align:center; margin-top:10px; color:#A1A1AA;">Sicherer Checkout über Alfima · Karte &amp; PayPal · Sofortiger Zugriff · 30-Tage-Geld-zurück-Garantie</p>
      </div>
    </div>
  </div>
</div>

<!-- BOOST POPUP -->
<div id="bp-overlay" role="dialog" aria-modal="true" aria-labelledby="bp-h">
  <div class="bp-modal">
    <button type="button" class="bp-close" id="bp-close" aria-label="Close">✕</button>
    <span class="bp-kicker">Kurze Frage</span>
    <h3 class="bp-h" id="bp-h">Schon mal „Boosten“ gedrückt — und nichts bekommen?</h3>
    <p class="bp-p">50 €. 200 Likes. Null neue Kunden. Das war keine Anzeige — <strong>das war eine Spende an Meta</strong>. Es gibt ein Basis-Setup, das aus Boosts echte Kampagnen macht — und die ersten 2 Lektionen sind offen.</p>
    <button type="button" class="btn-dark" id="bp-cta" data-free-class style="display:flex; width:100%; justify-content:center; text-align:center;">Die ersten 2 Lektionen kostenlos ansehen →</button>
    <p class="bp-sub">Keine Karte nötig · Nach Lektion 2 ist Ihr Meta-Business-Konto live</p>
    <button type="button" class="bp-later" id="bp-later">Vielleicht später</button>
  </div>
</div>

<script>
(function () {
  var UNLOCK_KEY = "gr_free_class_v1";

  var overlay    = document.getElementById("fc-overlay");
  var formState  = document.getElementById("fc-form-state");
  var videoState = document.getElementById("fc-video-state");
  var form       = document.getElementById("fc-form");
  var submitBtn  = document.getElementById("fc-submit");
  var errorEl    = document.getElementById("fc-error");
  var player     = document.getElementById("fc-player");

  async function sha256(value) {
    var normalized = (value || "").toLowerCase().trim();
    if (!normalized) return "";
    var buffer = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(normalized));
    return Array.from(new Uint8Array(buffer)).map(function (b) { return b.toString(16).padStart(2, "0"); }).join("");
  }
  function getCookie(name) {
    var m = document.cookie.match("(^|;)\\s*" + name + "\\s*=\\s*([^;]+)");
    return m ? m.pop() : "";
  }
  function getFbc() {
    var fbc = getCookie("_fbc");
    if (fbc) return fbc;
    var m = window.location.search.match(/[?&]fbclid=([^&]+)/);
    return m ? "fb.1." + Date.now() + "." + decodeURIComponent(m[1]) : "";
  }
  function getUrlParams() {
    var params = {};
    new URLSearchParams(window.location.search).forEach(function (v, k) { if (v) params[k] = v; });
    return params;
  }

  function isUnlocked() {
    try { return !!localStorage.getItem(UNLOCK_KEY); } catch (e) { return false; }
  }
  function renderState() {
    var unlocked = isUnlocked();
    formState.style.display  = unlocked ? "none" : "block";
    videoState.style.display = unlocked ? "block" : "none";
  }
  function openModal() {
    renderState();
    overlay.classList.add("open");
    document.body.style.overflow = "hidden";
  }
  function closeModal() {
    overlay.classList.remove("open");
    document.body.style.overflow = "";
    if (player) player.pause();
  }

  var triggers = document.querySelectorAll("[data-free-class]");
  for (var i = 0; i < triggers.length; i++) {
    triggers[i].addEventListener("click", function (e) { e.preventDefault(); openModal(); });
  }
  document.getElementById("fc-close").addEventListener("click", closeModal);
  overlay.addEventListener("click", function (e) { if (e.target === overlay) closeModal(); });
  document.addEventListener("keydown", function (e) {
    if (e.key === "Escape" && overlay.classList.contains("open")) closeModal();
  });

  form.addEventListener("submit", async function (e) {
    e.preventDefault();
    var fullName = document.getElementById("fc-name").value.trim();
    var email    = document.getElementById("fc-email").value.trim().toLowerCase();

    if (!fullName || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
      errorEl.textContent = "Bitte geben Sie Ihren Namen und eine gültige E-Mail an.";
      errorEl.style.display = "block";
      return;
    }
    errorEl.style.display = "none";
    submitBtn.disabled = true;
    var originalLabel = submitBtn.textContent;
    submitBtn.textContent = "Wird geöffnet…";

    var nameParts = fullName.split(/\s+/);
    var eventId   = Date.now() + "_" + Math.floor(Math.random() * 1e15);
    var emHash = await sha256(email);
    var fnHash = await sha256(nameParts[0]);
    var lnHash = await sha256(nameParts.slice(1).join(" "));
    var fbp = getCookie("_fbp");
    var fbc = getFbc();

    try {
      var res = await fetch("/api/leads", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          leadType: "free_class",
          product:  "launch-ads-blueprint",
          fullName: fullName,
          email:    email,
          meta_event_id:         eventId,
          meta_em:               emHash,
          meta_fn:               fnHash,
          meta_ln:               lnHash,
          meta_fbp:              fbp,
          meta_fbc:              fbc,
          meta_user_agent:       navigator.userAgent,
          meta_event_source_url: window.location.href,
          submitted_at:          new Date().toISOString(),
          marketing_consent:     !!window.__marketingConsent,
          url_params:            getUrlParams(),
        }),
      });
      if (!res.ok) throw new Error("bad status");

      try { localStorage.setItem(UNLOCK_KEY, JSON.stringify({ email: email, at: new Date().toISOString() })); } catch (err) {}

      if (typeof window.trackMarketing === "function") {
        window.trackMarketing("Lead", { content_name: "Free Lesson 1", content_category: "free_class" }, { eventID: eventId });
      }
      window.dataLayer = window.dataLayer || [];
      window.dataLayer.push({
        event: "free_class_lead",
        event_id: eventId,
        em: emHash, fn: fnHash, ln: lnHash,
        fbp: fbp, fbc: fbc,
      });

      renderState();
      if (player && player.play) player.play().catch(function () {});
    } catch (err) {
      errorEl.textContent = "Etwas ist schiefgelaufen. Bitte versuchen Sie es erneut.";
      errorEl.style.display = "block";
    } finally {
      submitBtn.disabled = false;
      submitBtn.textContent = originalLabel;
    }
  });

  // ── Free lessons: player com aulas 1 e 2 ────────────────────────────
  var LESSONS = [
    { label: "Lektion 1 von 20 — offene Vorschau", title: "Why most beginners fail.",        src: "/videos/lesson-1.mp4" },
    { label: "Lektion 2 von 20 — offene Vorschau", title: "Who the Meta Ads course is for.", src: "/videos/lesson-2.mp4" },
  ];
  var currentLesson = 0;
  var milestones = { 25: false, 50: false, 75: false, 95: false };

  function playLesson(i, autoplay) {
    currentLesson = i;
    milestones = { 25: false, 50: false, 75: false, 95: false };
    document.getElementById("fc-eyebrow").textContent = LESSONS[i].label;
    document.getElementById("fc-lesson-title").textContent = LESSONS[i].title;
    var nextCta = document.getElementById("fc-next-cta");
    if (nextCta) nextCta.style.display = i === 0 ? "flex" : "none";
    var rows = document.querySelectorAll(".fc-lesson.fc-playable");
    for (var r = 0; r < rows.length; r++) {
      var active = Number(rows[r].dataset.lesson) === i;
      rows[r].classList.toggle("active", active);
      rows[r].querySelector(".fc-tag").textContent = active ? "▶ Läuft gerade" : "▶ Kostenlos ansehen";
    }
    if (player.getAttribute("src") !== LESSONS[i].src) player.setAttribute("src", LESSONS[i].src);
    if (autoplay && player.play) player.play().catch(function () {});
  }

  document.querySelectorAll(".fc-lesson.fc-playable").forEach(function (row) {
    row.addEventListener("click", function () { playLesson(Number(row.dataset.lesson), true); });
  });

  var nextCtaEl = document.getElementById("fc-next-cta");
  if (nextCtaEl) {
    nextCtaEl.addEventListener("click", function () { playLesson(1, true); });
  }

  // ── Aulas bloqueadas: clique mostra aviso + CTA de compra ────────────
  var lockNote = document.getElementById("fc-lock-note");
  document.querySelectorAll(".fc-lesson.locked").forEach(function (row) {
    row.addEventListener("click", function () {
      var num = row.querySelector(".num").textContent;
      var title = Array.prototype.filter
        .call(row.childNodes, function (n) { return n.nodeType === 3; })
        .map(function (n) { return n.textContent; }).join("").trim();
      document.getElementById("fc-lock-text").innerHTML =
        "Lektion " + num + " — <strong>" + title + "</strong> — ist Teil des vollständigen Kurses. " +
        "Schalten Sie alle 18 übrigen Lektionen + Boni frei — einmalig <strong>27 €</strong> <s>197 €</s>.";
      row.after(lockNote);
      lockNote.style.display = "block";
      window.dataLayer = window.dataLayer || [];
      window.dataLayer.push({ event: "free_class_locked_click", lesson: Number(num) });
    });
  });

  // ── Video progress → dataLayer (25/50/75/95%) ───────────────────────
  if (player) {
    player.addEventListener("ended", function () {
      if (currentLesson === 0) { playLesson(1, true); return; }
      var offer = document.getElementById("fc-offer");
      if (!offer) return;
      offer.scrollIntoView({ behavior: "smooth", block: "center" });
      offer.classList.add("fc-attn");
      setTimeout(function () { offer.classList.remove("fc-attn"); }, 4000);
    });
    player.addEventListener("timeupdate", function () {
      if (!player.duration) return;
      var pct = (player.currentTime / player.duration) * 100;
      for (var mark in milestones) {
        if (!milestones[mark] && pct >= Number(mark)) {
          milestones[mark] = true;
          window.dataLayer = window.dataLayer || [];
          window.dataLayer.push({ event: "free_class_progress", lesson: currentLesson + 1, video_percent: Number(mark) });
          if (mark === "50" && typeof window.trackMarketing === "function") {
            window.trackMarketing("ViewContent", { content_name: "Free Lesson " + (currentLesson + 1), content_category: "free_class" });
          }
        }
      }
    });
  }
})();
</script>

<!-- Cookie Banner -->
<div id="cookie-banner" style="position:fixed; left:16px; right:16px; bottom:16px; z-index:9999; display:none; max-width:560px; margin:0 auto; background:#fff; color:var(--body); border:1px solid var(--line); border-radius:14px; box-shadow:0 12px 40px rgba(0,0,0,.12); padding:18px 20px;">
  <p style="font-size:13px; line-height:1.5; margin-bottom:14px;">
    Wir verwenden Cookies, um Ihre Erfahrung zu verbessern und unser Marketing zu messen. Mehr dazu in unserer
    <a href="/privacy-policy" style="color:var(--accent-ink); text-decoration:underline;">Datenschutzerklärung</a>.
  </p>
  <div style="display:flex; gap:10px;">
    <button id="cookie-reject" type="button" style="flex:1; background:transparent; color:var(--body); border:1px solid var(--line); border-radius:8px; padding:10px; font-size:13px; font-weight:600; cursor:pointer; font-family:inherit;">Ablehnen</button>
    <button id="cookie-accept" type="button" style="flex:1; background:var(--ink); color:#fff; border:none; border-radius:8px; padding:10px; font-size:13px; font-weight:600; cursor:pointer; font-family:inherit;">Akzeptieren</button>
  </div>
</div>
<script>
  (function () {
    var KEY = "gr_cookie_consent_v1";
    var banner = document.getElementById("cookie-banner");
    if (!banner) return;
    function hide() { banner.style.display = "none"; }
    function save(v) { try { localStorage.setItem(KEY, v); } catch (e) {} }
    var choice = null;
    try { choice = localStorage.getItem(KEY); } catch (e) {}
    if (!choice) {
      banner.style.display = "block";
    }
    document.getElementById("cookie-accept").addEventListener("click", function () {
      save("accepted");
      window.__marketingConsent = true;
      if (typeof gtag === "function") gtag('consent', 'update', { 'analytics_storage': 'granted' });
      hide();
    });
    document.getElementById("cookie-reject").addEventListener("click", function () {
      save("rejected");
      window.__marketingConsent = false;
      if (typeof gtag === "function") gtag('consent', 'update', { 'analytics_storage': 'denied' });
      hide();
    });
  })();
</script>
<!-- End Cookie Banner -->

<!-- Passa atribuição (fbclid, utm_*, _fbp, _fbc) para o checkout (cross-domain) -->
<script>
  (function () {
    function getCookie(name) {
      var m = document.cookie.match("(^|;)\\s*" + name + "\\s*=\\s*([^;]+)");
      return m ? m.pop() : "";
    }
    function buildCheckoutUrl(base) {
      try {
        var url = new URL(base, window.location.origin);
        var current = new URLSearchParams(window.location.search);
        current.forEach(function (value, key) { if (value) url.searchParams.set(key, value); });
        var fbp = getCookie("_fbp");
        var fbc = getCookie("_fbc");
        if (fbp) url.searchParams.set("fbp", fbp);
        if (fbc) {
          url.searchParams.set("fbc", fbc);
        } else if (current.get("fbclid")) {
          url.searchParams.set("fbc", "fb.1." + Date.now() + "." + current.get("fbclid"));
        }
        url.searchParams.set("consent", window.__marketingConsent ? "granted" : "denied");
        return url.toString();
      } catch (e) { return base; }
    }
    function refreshLinks() {
      var links = document.querySelectorAll("a[href*='alfima.com']");
      for (var i = 0; i < links.length; i++) {
        if (!links[i].dataset.base) links[i].dataset.base = links[i].getAttribute("href");
        links[i].setAttribute("href", buildCheckoutUrl(links[i].dataset.base));
      }
    }
    refreshLinks();
    document.addEventListener("click", function (e) {
      var a = e.target && e.target.closest ? e.target.closest("a[href*='alfima.com']") : null;
      if (a) a.setAttribute("href", buildCheckoutUrl(a.dataset.base || a.getAttribute("href")));
    }, true);
  })();
</script>
<!-- End attribution passthrough -->

<script>
  (function () {
    var links = document.querySelectorAll("[data-setlang]");
    for (var i = 0; i < links.length; i++) {
      links[i].addEventListener("click", function (e) {
        e.preventDefault();
        try { localStorage.setItem("gr_lang", this.dataset.setlang); } catch (err) {}
        location.href = this.getAttribute("href") + location.search;
      });
    }
  })();
</script>

<script>
(function () {
  var SEEN_KEY = "gr_boost_pop_v1";
  var overlay = document.getElementById("bp-overlay");
  if (!overlay) return;
  function seen() { try { return !!localStorage.getItem(SEEN_KEY); } catch (e) { return true; } }
  function mark() { try { localStorage.setItem(SEEN_KEY, "1"); } catch (e) {} }
  function isLead() { try { return !!localStorage.getItem("gr_free_class_v1"); } catch (e) { return false; } }
  function close() { overlay.classList.remove("open"); }

  setTimeout(function () {
    if (seen() || isLead()) return;
    var fc = document.getElementById("fc-overlay");
    if (fc && fc.classList.contains("open")) return; // funil já aberto
    overlay.classList.add("open");
    mark(); // uma vez por visitante
    window.dataLayer = window.dataLayer || [];
    window.dataLayer.push({ event: "boost_popup_shown" });
  }, 10000);

  overlay.addEventListener("click", function (e) { if (e.target === overlay) close(); });
  document.getElementById("bp-close").addEventListener("click", close);
  document.getElementById("bp-later").addEventListener("click", close);
  document.getElementById("bp-cta").addEventListener("click", function () {
    close();
    window.dataLayer = window.dataLayer || [];
    window.dataLayer.push({ event: "boost_popup_cta" });
  });
  document.addEventListener("keydown", function (e) {
    if (e.key === "Escape" && overlay.classList.contains("open")) close();
  });
})();
</script>
</body>
</html>
